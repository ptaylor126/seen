# Seen — Session Journal

Running session log. Newest entries at top. Read this first to brief future Claude Code sessions on current state.

---

## Tech debt

Cleanup items deferred from other work — not blocking, but don't lose track.

- `supabase/functions/send-push-notification/index.ts` has a `case 'friend_request'` branch (around lines 43 and 274–283) that became unreachable after `20260603120000_drop_friend_request_notification_trigger` removed the only producer of `kind='friend_request'` notification rows. Cleanup pass: delete the branch and any helper code only it referenced.
- `notifications_kind_check` CHECK constraint still allows `'friend_request'` as a permitted `kind` value. Harmless given there's no inserter, but it would silently mask a reintroduction bug. Follow-up migration could drop `'friend_request'` from the allowed set.
- Badge friend-request behavior (persists until accept/decline, not cleared by opening inbox) is code-verified only — not yet tested on device, no second account to send a request. Watch once more testers are on.
- "Friends are watching" multi-watcher stacking, +N overflow, and count-based ordering are code-verified only — can't exercise with one friend. Confirm once multiple testers have overlapping watching items.
- Library leak bug: items RLS is permissive (own + friends' public rows) by design. Any "just my stuff" query must filter user_id explicitly — never rely on RLS as the scoping filter.

---

## Open items / UX

Product or interaction gaps that need a decision, not a code cleanup. Land in a dated entry once resolved.

- Toggle-off replaced the re-tap-to-re-rate path on title detail. No way to change an existing rating now without removing and re-adding. Add a dedicated "edit rating" affordance — decide placement (tap stars / long-press / pencil) so it doesn't conflict with the toggle-off tap.
- Splash fix (preventAutoHideAsync timing + imageWidth) committed but UNVERIFIED — only observable in a dev/EAS build, not Expo Go. Confirm it actually shows correctly next time a build is cut. Low priority.

---

## 2026-06-03

**Done**
- "Friends are watching" restructured to social proof — grouped by show, watcher avatars stacked (cap 5 + N), sorted by watcher count DESC then most-recent activity DESC, Watching status only (dropped Watched). Query shape unchanged (3 round-trips: friend items + watcher profiles + TMDB metadata, all batched); status filter narrowed from `.in(['watching','watched'])` to `.eq('watching')` and limit bumped 40 → 200 so popular shows don't have their watcher counts clipped by the window. Grouping done in memory via `Map<"mediaType:tmdbId", { watcherIds[], mostRecentAt }>` from the already-ordered SQL result; ties broken by `mostRecentAt.localeCompare`. Tile render: 18pt stacked-avatar chips (down from a single 24pt avatar), 11pt negative `marginLeft` (~60% overlap), +N pill in `palette.accent` at the leftmost slot (back of stack), most-recent watcher rightmost (front of stack). FriendCard type lost its singular `sender`/`attribution` fields in favour of `watchers[]` + `totalWatchers`. Multi-watcher stacking, +N overflow, and count-based ordering are CODE-VERIFIED ONLY — can't test with one friend. Confirm the stacked-avatar visual and ordering once multiple testers are watching overlapping shows.
- Header convergence: Library/Friends/Profile/Inbox now share `ScreenHeader` (`dd2b8ab`). Before: Library was bespoke (left-aligned, `typography.display` 32pt); Friends/Profile/Inbox were the shared `ScreenHeader` rendering a centered 3-column flex grid at `typography.heading` (22pt). After: `ScreenHeader` rewritten to a left-aligned `typography.display` title with a `rightActions?: ReactNode` slot for per-screen icons appended before the Mail bell. Dropped the fixed `HEADER_HEIGHT = 44` — display's 38pt lineHeight wouldn't fit; bar is now padding-driven (`paddingVertical: spacing.md` → ~62pt actual). Library moved onto `ScreenHeader` with `rightActions={<Search/><Plus/>}` for its non-searching mode; its search-mode `TextInput` row stays bespoke (it's a complete header replacement, not a title tweak). Friends/Profile/Inbox: zero call-site changes — they pick up the new style automatically. **Inbox decision**: did NOT add a `compact?` opt-out for Inbox. Treating the converged style as universal — if a screen really can't fit, it replaces the header entirely (like Library's search mode) rather than mutating `ScreenHeader`.
- Friends invite action moved to a floating centered pill above the tab bar (`8ecdc37`). Replaced the top-right "Invite more" text link with a `position: absolute` pill at `bottom: spacing.base`, `paddingHorizontal: spacing.lg`, `borderRadius: radius.full`, accent fill, hugging its label (centered in a full-width `box-none` wrapper). Reason: a long friends list could push the invite action off-screen; floating it guarantees the CTA stays reachable. Wrapper uses `pointerEvents="box-none"` so taps on its transparent padding pass through to the list scrolling underneath; only the pill itself is tappable. `listContent.paddingBottom` bumped from `spacing.lg` to `spacing.xxxl + spacing.base` so the last friend isn't hidden behind the button. **Tab bar / safe-area note**: React Navigation's bottom-tabs renders the tab bar *outside* the screen view and already extends through the home indicator, so `bottom: spacing.base` inside the screen sits exactly above the tab bar's top edge — no `useSafeAreaInsets()` needed (it would double-count and push the pill visibly higher). Same handler as the old link (`router.push('/friends/invite')`) — no behavior change, only placement.
- Sign-in screen: replaced "Seen" text wordmark with the logo PNG, native splash now held through font load, refreshed icon/logo assets (`814b3d5`). Logo is 1998×587 RGBA (transparent bg) — sharp on retina, blends with cream `palette.bg` without a box; rendered via `expo-image` with `contentFit="contain"` at `min(240, screenWidth * 0.6)`, height derived from the 3.40:1 aspect ratio. Splash hold: added `SplashScreen.preventAutoHideAsync()` at module scope in `_layout.tsx` + `SplashScreen.hideAsync()` in an effect gated on `fontsLoaded`. Previously the splash auto-hid on the first React frame mount (even the `null` font-loading render), flashing the cream background before Geist resolved. Also made `imageWidth: 240` explicit in the `expo-splash-screen` plugin config to match the sign-in logo's on-screen size. **Verifiable only in a dev/EAS build, not Expo Go** — Expo Go shows its own launch screen and ignores the plugin-configured custom splash. Logged under Open items / UX as UNVERIFIED until the next build is cut. Same commit refreshed `assets/icon.png`, `assets/logo.png`, and `assets/logo-black.png` (the previous repo versions were small placeholders).
- Chrome polish across home, title detail, and recommend modal (`75f5dff`). (A) Home's friends-are-watching grid: dropped `justifyContent: 'space-between'` and switched to `columnGap: spacing.base, rowGap: spacing.base` so the row left-aligns naturally without odd inter-tile gaps. `rowGap` matters because the grid wraps once more than ~4 friends are visible — column gap alone would let rows touch. (B) Title detail's close button: replaced `insets.top + spacing.sm` with `closeButtonTop: spacing.base` so the X sits tight to the top-right corner instead of inset by safe-area + margin. (C) Recommend modal horizontal padding: all six horizontal-inset spots (header, titleContext, sectionLabel, friendList, noteBox marginHorizontal, charCount) bumped from `spacing.base` to `spacing.lg` for a more generous inset. (D) Swapped `Bell` → `Mail` (lucide) in both `src/components/screen-header.tsx` and `src/app/(tabs)/library.tsx`'s inbox button — "mail" reads more accurately than "bell" for a per-user inbox of recs and friend requests, less of an alarm metaphor.
- Declare `usesNonExemptEncryption: false` in `app.json` (`444f657`). Added under `ios.config`. App uses only standard HTTPS/TLS — no custom crypto — so this clears App Store Connect's export-compliance prompt automatically on every upload instead of requiring a manual answer per build.
- Badge semantics: count pending friend requests separately, drop the friend_request notification trigger (`9c71238`). **Problem**: the inbox badge counted unread `notifications` rows. The `friend_requests_notify_received` trigger wrote a `kind='friend_request'` notification row alongside every new `friend_requests` row, so opening the inbox (which marks all unread notifications read on focus) dropped the badge to zero even when the underlying friend request was still pending action. **Two options considered**: (A) keep the trigger, exclude `kind='friend_request'` from the unread-count query so inbox focus can't clear it; (B) drop the trigger entirely and count `friend_requests WHERE to_user_id = me` directly, treating each table as its own source of truth. **Chose B** — per-table source of truth is cleaner than carrying a redundant "this row is the action item, ignore me, look over there" notification row. The conceptual split: `notifications` = *things to see* (informational, clear on view — `rec_received` / `rec_watched` / `friend_accepted`); `friend_requests` = *things to act on* (row existence IS the pending state; accept/decline both DELETE). Recs are informational — "seen" is a complete state for a recommendation, nobody's blocked on it — so clear-on-view is correct. Friend requests require action, so they persist regardless of inbox visits until accept/decline. **Behavioural shift to note**: the prior code counted `recommendations.status='pending'`, which would have persisted past inbox view; the new code counts unread `notifications` (including `rec_received`), which clears on view. Confirmed as the desired model. **Implementation** (migration `20260603120000_drop_friend_request_notification_trigger`): drop trigger + helper function, `delete from notifications where kind='friend_request'` to remove transitional double-count rows, idempotent `ALTER PUBLICATION supabase_realtime ADD TABLE` for both `notifications` and `friend_requests` (guarded with `pg_publication_tables` lookups so it doesn't 42710-error on environments where the tables were toggled into the publication via the Supabase dashboard). `src/hooks/use-unread-count.ts` rewritten: two parallel count queries summed (`notifications.read_at IS NULL` + `friend_requests.to_user_id = me`), plus a realtime channel `unread:${userId}` subscribed to `postgres_changes` on both tables (filtered to the current user) — refetches the full count on any event rather than maintaining a delta. RLS audit PASS — both tables already have user-scoped SELECT policies that Supabase realtime enforces before delivering events, so subscribers only get rows they could already SELECT. Two follow-up tech-debt items logged in the top-of-journal **Tech debt** section: the now-unreachable `case 'friend_request'` branch in `supabase/functions/send-push-notification/index.ts`, and `notifications_kind_check` still permitting `'friend_request'` as a kind.

**Decisions worth remembering**
- **Header system is unified on `ScreenHeader`**: left-aligned title at `typography.display`, padding-driven height (no fixed `HEADER_HEIGHT`), `rightActions?: ReactNode` slot for per-screen icons appended before the Mail bell. Every tab/screen header goes through this component — no per-screen sizing variants, no `compact?` opt-out. If a screen genuinely can't fit the shared shape (Library's search-mode `TextInput` row is the only current example), it replaces the header entirely for that mode rather than adding a knob to `ScreenHeader`. New screens: use `ScreenHeader` with `rightActions` if you need extra icons; don't roll a bespoke header for the title-style case.
- **Floating CTAs above the tab bar**: React Navigation's bottom-tabs renders the tab bar outside the screen view and already extends through the home indicator. A `position: absolute` element inside the screen view at `bottom: spacing.base` sits exactly above the tab bar's top edge — do NOT add `useSafeAreaInsets().bottom`, it double-counts and creates a visible gap. If the floating element overlaps a scrolling list, give the list a matching `paddingBottom` so its last row clears the CTA, and put `pointerEvents="box-none"` on the floating wrapper so its transparent padding doesn't block list taps.
- **Badge semantics**: split badge sources by action model, not by notification taxonomy. Informational events (rec received, rec watched, friend accepted) clear on view; action-required state (pending friend requests) persists until actioned. If a future feature needs an action-required signal, prefer counting the action-state table directly over routing it through `notifications` with an "exclude this kind" filter — the filter is a tell that the kind shouldn't have been in `notifications` to begin with.
- **Publication membership must be explicit AND defensive**: for any table that needs realtime delivery in the app, the migration that introduces the dependency must add the table to `supabase_realtime`. Guard `ALTER PUBLICATION ... ADD TABLE` with a `pg_publication_tables` lookup so it's safe against environments where the dashboard has already toggled the table in (otherwise SQLSTATE 42710 blows up the migration mid-deploy).
- **App Store export compliance**: any new project ships with `usesNonExemptEncryption: false` in `app.json` from day one unless we're actually using custom crypto — eliminates the per-upload prompt without committing us to anything (it just declares we're inside the standard-HTTPS exemption).

**Next**
- Cut a new EAS build (chrome polish + export-compliance + badge migration all need to ship together; the migration also needs to run against the production Supabase project before the new build is exercised against it).
- Set up external TestFlight distribution. First external build triggers Apple's Beta App Review (typically 24–48 hours), so factor that into release timing.

**Open questions**
- None.

**Session close**

*Shipped*
- Library: list/grid view modes (2/3/4 density, global preference persisted to AsyncStorage), recommender attribution chips + rating chips, grouped view controls (filled list/grid toggle, stroked density numbers), Reanimated switch animation.
- Home: "Friends are watching" restructured to social proof — grouped by show, stacked watcher avatars (cap 5 + N), sorted by watcher count then recency, Watching status only.
- Shared `SearchBar` component on both Home and Library (TMDB search + add); removed Library's Plus icon and local-rows filter; consolidated header.
- Avatar fallback: deterministic color-by-id, all sites seeded on userId, consistent color per person everywhere including the Friends list.
- Sign-in logo wordmark; splash hold; asset refresh; floating pill invite button on Friends; unified `ScreenHeader` across Library / Friends / Profile / Inbox.
- Fixed earlier: library tab leaking friends' items (explicit `user_id` filter), toggle-off to remove library items, badge counts pending friend requests separately.

*Verified-only — NOT tested at scale; confirm with more testers*
- Multi-watcher avatar stacking + "+N" overflow (needs several friends on one show).
- Social-proof ordering by watcher count (needs varying counts across shows).
- Avatar color differentiation across many users (only one friend so far → only two distinct hashes in play).
- Friend-request badge persisting until actioned (no second account to test).
- Splash and app icon (only render in a real dev/EAS build, not Expo Go).

*Tech debt / lessons*
- **LayoutAnimation no-ops under SDK 54's New Architecture (Fabric).** Mount/unmount animations registered via `LayoutAnimation.configureNext` run silently with no visible effect. Use `react-native-reanimated` `entering` / `exiting` / `layout` props instead — they work on both architectures. Applies to any future "thing appears next to other thing" animation.
- **`items` RLS is permissive by design** (own + friends' public rows). Any "just my stuff" query must filter `user_id` explicitly; never rely on RLS as the scoping filter.
- Removed Library's local-rows substring filter deliberately when the shared SearchBar took over. Revisit if a long-library filter becomes useful — it would coexist with the TMDB search bar but needs its own affordance.
- v2 idea: Letterboxd CSV import (title + year → TMDB match), films-only, good cold-start lever for new users with existing watch history.

*Next*
- Cut a new EAS build with all of the above, supersede the build currently in External Beta, then share the TestFlight link.

---

## 2026-05-21

**Done**
- Onboarding keyboard avoidance + best-watched copy simplification + profile-load silent retry (`5d1c031`). (A) Wrapped each of the 5 onboarding screens with text inputs (handle, display-name, last-watched, best-watched, currently-watching) in `KeyboardAvoidingView` — `behavior='padding'` on iOS with `keyboardVerticalOffset={insets.bottom}`. The offset cancels with the SafeAreaView's bottom inset so the Continue button sits at the footer's `paddingBottom` (12px) above the keyboard instead of safe-area + footer-padding (~46px). Android passes `behavior={undefined}` and relies on the manifest's `windowSoftInputMode="adjustResize"`. (B) Replaced best-watched's body subtitle "Rate it — your friends can see your taste, and we'll suggest things you might love." with the much shorter "Friends will see your favourites." Then dropped the conditional caption that displayed "Friends will see this as a recommendation/pass." once a rating was tapped — redundant now that the body conveys the same intent. Removed the now-unused `thumbFromRating` import too. (C) `useProfile` no longer transitions to an `'error'` status on fetch failure; instead `refresh()` retries indefinitely with capped exponential backoff (`1500 × 2^(attempt-1)` ms, capped at 30000 ms). A `generationRef` cancels superseded loops when `refresh()` is called again or on `SIGNED_OUT`. The root layout stays in its loading overlay during retries — transient network blips self-heal without dumping the user into a broken state.
- Onboarding progress redesign + search layout fix + TMDB auth diagnostic (`3ac47e0`). (A) Renamed `src/components/onboarding-dots.tsx` → `onboarding-progress.tsx` (git tracked as delete + create due to >40% rewrite; `git log --follow` still reaches old history). Visual changed from 6 dots at the bottom of each screen to a Stripe-style row of 6 linear bars at the top, just below the safe-area inset and above the header row. Bars: 3px tall, equal-width (`flex: 1`), `gap: spacing.xs` between, full screen width via horizontal padding matching the screen body. Past+current filled with `palette.accent` at opacity 1; future dimmed `palette.textMuted` at opacity 0.3. (B) Search-screen layout bug: on the three search steps (last-watched, best-watched, currently-watching), the OnboardingSearch's FlatList was overflowing past the footer and visually overlapping the Continue button. Root cause: RN flex children default to `minHeight: 'auto'` which equals their content height, so a `flex: 1` column with tall content won't actually shrink below the content. Fix: added `minHeight: 0` to `OnboardingSearch`'s container style + explicit `flex: 1` style on the FlatList, then `minHeight: 0` to each search screen's `body` and `searchWrap` styles. The constraint has to propagate up the column for the inner FlatList to know it can shrink. (C) Added a temporary `console.log` in `callProxy` (`src/lib/tmdb.ts`) to diagnose a persistent 401 from `tmdb-proxy` during onboarding's Last Watched search — logs `{ hasSession, hasToken, path }` immediately before `functions.invoke`.
- Onboarding Skip handlers + footer anchoring + diagnostic log format (`7ea36e0`). (A) Skip on `last-watched` was calling `finishOnboarding({ onComplete: () => router.replace('/(tabs)') })` — which exited onboarding instead of advancing AND tripped a race where the navigation could fire before `refreshProfile()`'s setState propagated through the Context. The root layout's effect would then re-run with stale `onboarded: false` + `segments: '(tabs)'`, hit the `!inOnboardingGroup` branch, and `router.replace('/(onboarding)/welcome')` — bouncing the user back to the start of onboarding. Same bug on best-watched's Skip. Fix: both Skip handlers now `router.push('/(onboarding)/<next-step>')` and advance forward like the other Continue buttons. `finishOnboarding` is now called only from `currently-watching` (the last step) via both Continue + Skip. Its `onComplete` parameter is gone entirely — the helper does DB update + `await refreshProfile()` and stops. The root layout's routing effect sees `onboarded: true` while we're still in `/(onboarding)` and redirects to `/(tabs)` cleanly with no race. Removed now-unused `useProfile` + `finishOnboarding` imports from last-watched and best-watched. (B) Added `marginTop: 'auto'` to the footer style in all 6 onboarding screens. The body's `flex: 1` already takes remaining space, but `marginTop: auto` on the next sibling is an explicit bottom-anchor that doesn't depend on flex distribution — robust against future layout tweaks that might inadvertently change the body's flex shape. (C) Tightened the diagnostic log shape: `console.log('[tmdb-proxy] auth state:', { hasSession, hasToken, tokenStart })` — `tokenStart` is the first 20 chars of `access_token`, used to tell apart "anon key" vs "real user JWT" (both start with `eyJ...`).
- Keyboard appearance + footer position + TMDB 401 root-cause fix (`d970ab0`, `32e5ca6`). Three issues in one go after seeing the user's screenshots. (A) **TMDB 401 root cause** in `supabase/functions/tmdb-proxy/index.ts`: the proxy was calling `supabaseClient.auth.getUser()` without an argument, expecting the `global.headers.Authorization` set on `createClient` to be used. It's not — supabase-js's GoTrue client maintains its OWN session and `auth.getUser()` (no-arg) uses that session, not the global headers. Since the proxy's fresh client has no session, `getUser()` returned no user and we 401'd, even when a valid user JWT was sitting right there in the global headers. Fix: extract the JWT explicitly (`authHeader.slice('Bearer '.length)`) and call `auth.getUser(jwt)` — validates the passed-in JWT against the auth server unambiguously. Added a server-side `console.warn` on rejection so any future 401s log the JWT prefix + auth error to Edge Function logs. Deployed. Removed the client-side diagnostic `console.log` since it's no longer needed. (B) **Footer position above the keyboard** — the previous `keyboardVerticalOffset={insets.bottom}` was meant to cancel the SafeAreaView's bottom inset but didn't behave as documented (the prop semantically describes "distance from screen top to KAV top", not "subtract from bottom"). Switched all 6 onboarding screens to: `SafeAreaView edges={['top']}` (drop bottom), KAV with default offset (0), new `useKeyboardOpen` hook (`src/hooks/use-keyboard-open.ts`) that subscribes to `keyboardWillShow`/`Hide`, and dynamic footer `paddingBottom: keyboardOpen ? spacing.md : insets.bottom + spacing.md`. Now: keyboard closed → 12+34=46pt above home indicator; keyboard open → 12pt above keyboard. Also dropped the `marginTop: 'auto'` from footer styles introduced in `7ea36e0` — turns out it wasn't actually anchoring anything since body's `flex: 1` was already doing the job. (C) **Keyboard appearance flash** — TextInputs in handle, display-name, and onboarding-search initially defaulted to system keyboard appearance (could pick up brief 'dark' from `useColorScheme()` during the welcome→handle transition, then flip to light). First fix attempt (`d970ab0`) set `keyboardAppearance={scheme === 'dark' ? 'dark' : 'light'}` — still flashed dark briefly because the ternary could resolve to 'dark' before `useColorScheme()` settled to its final value. Second fix (`32e5ca6`) hardcoded `keyboardAppearance="light"` everywhere — can't flash dark when the prop is never 'dark'. The app's only colour scheme in practice today is light, so no behavioural regression.
- Confirmation card replaces search after item added on onboarding's single-item steps (`d2a64dc`). `last-watched.tsx` and `currently-watching.tsx` previously kept the OnboardingSearch + FlatList visible *and* showed a small "Added" line below — users didn't realise they were done and kept typing. Now the search is rendered only when `added === null`; once an item is picked, the search disappears entirely and a centered card takes its place: "Added" caption + title (`typography.bodyEmphasis`) + a `Pick something else` Pressable in `palette.accent` that calls `setAdded(null)` to restore the search. `best-watched.tsx` already had this `picked ? card : search` conditional pattern — just added the "Added" caption above the title to match. Card styles unified across the three: `padding: spacing.lg, borderRadius: radius.md, gap: spacing.md, marginTop: spacing.md, alignItems: 'center'` (matches what best-watched already had).
- LogBox-suppress AuthApiError + FlatList shrink fix + poster in confirmation card (`00f36b4`). (A) `src/lib/supabase.ts` adds `LogBox.ignoreLogs(['Invalid Refresh Token', 'Refresh Token Not Found', 'AuthApiError'])` — supabase-js logs these as `console.error` on background refresh failure and RN's LogBox surfaces them as a red dev toast on the sign-in screen. The cleanup behaviour (session clear + `SIGNED_OUT`) is unchanged; this is purely cosmetic suppression for dev, no effect in production. (B) `src/components/onboarding-search.tsx` FlatList style gains `minHeight: 0` — without it the FlatList ignored its parent's flex bound and overflowed the searchWrap when a single ~84pt result row was taller than the squeezed-by-keyboard search area, pushing Continue up into the row. (C) `last-watched.tsx`, `best-watched.tsx`, `currently-watching.tsx`: `AddedItem`/`PickedItem` gain `posterPath: string`; the confirmation/picked card now renders an `<Image>` between the "Added" caption and the title at 100×150 with `borderRadius: radius.sm`. The poster_path is already on `SearchableItem` (filtered to non-null at type level), so the upsert just threads it through.
- Best-watched heading copy softened (`82bc3e3`): "What's the best thing you've watched recently?" → "Watched anything good lately?" Single-line, more conversational. Body subtitle ("Friends will see your favourites.") unchanged.
- Lock app to light mode + last-watched NBSP + verbose proxy diagnostics (`30cf6d9`). (A) `app.json`: `userInterfaceStyle: "automatic"` → `"light"`. The hardcoded `keyboardAppearance="light"` props alone weren't enough — iOS reads keyboard appearance from the app's UI traits at the moment the keyboard is requested, which can lag the bridged prop by a frame or two. Setting userInterfaceStyle locks the trait at the iOS level so the keyboard defaults to light from the first frame. **Caveat**: this is a native config — JS hot reload doesn't propagate it. Requires a full native rebuild (`npx expo prebuild --clean && npx expo run:ios` or fresh EAS dev build) to take effect on device. (B) `last-watched.tsx` body text: regular space between "add" and "it." replaced with `{' '}` (NBSP) so "add it." stays together on one line and the period doesn't orphan onto its own line at narrow widths. (C) `tmdb-proxy/index.ts` gains verbose `console.log`/`console.warn` on every request — incoming header prefix, JWT prefix on rejection, `userPresent` flag, and a success log with `user.id`. Plus a matching client-side `console.log('[tmdb-proxy] callProxy:', { path, hasSession, hasToken, tokenStart, expiresAt })` in `src/lib/tmdb.ts` so client+server logs can be correlated to pinpoint where a 401 originates.
- TMDB 401 root-caused + auto-signout (`c0ab587`). Diagnostic correlation revealed the actual failure mode: the JWT being sent is structurally valid (correct prefix, present access token) but `auth.getUser(jwt)` server-side returns `"User from sub claim in JWT does not exist"`. This happens when the Supabase user the JWT references has been deleted (during dev: manually via dashboard to test fresh onboarding; in production: via the `delete_account` Edge Function). supabase-js's auto-refresh only fires on access-token expiry, and the cached refresh token is itself tied to the dead user — so even when refresh eventually triggers, it errors as `"Invalid Refresh Token"` (which we'd already silenced via LogBox). Net effect: the stale session sits in AsyncStorage forever, 401-ing every TMDB call, with no recovery path until manual reinstall. **Fix**: in `callProxy`'s error handler, when the response status is 401, `await supabase.auth.signOut()` before throwing. This clears the bad session + AsyncStorage and fires `SIGNED_OUT`, which the root layout's routing effect picks up to route to `/(auth)/sign-in`. Re-signing in with Apple mints a fresh session against the now-current user. No reinstall required, transparent recovery.
- Revert dynamic footer padding back to constant (`268a43f`). Restored all 6 onboarding screens to: `SafeAreaView edges={['top', 'bottom']}` + KAV `keyboardVerticalOffset={insets.bottom}` + constant `paddingBottom: spacing.md` on footer. Dropped the `useKeyboardOpen` import and inline-dynamic style array. **Why**: the dynamic approach from `d970ab0` had a discontinuous footer-paddingBottom transition (46pt → 12pt instantly on `keyboardWillShow`) that caused the Continue button to visually "come from behind the keyboard" during the animation — the instant 34pt drop preceded KAV's smooth padding-animate. Constant footer paddingBottom + KAV's own smooth animation = single coherent motion. Final gap to keyboard ended up bigger than intended though — see follow-up commit.
- Wrap onboarding bodies in ScrollView and animate footer paddingBottom with keyboard slide (`de074d3`). The previous revert traded one problem for another: the constant footer padding gave a smooth animation but the gap to the keyboard was now too big (~80pt), and the search result row was being squeezed by the fixed-flex layout when keyboard was up. Two interlocking changes: (A) **`useKeyboardOpen` hook upgraded** to call `LayoutAnimation.configureNext({ duration: e.duration })` in each `keyboardWillShow`/`Hide` handler. Any layout change in the same render — including the footer's `paddingBottom` switching between `spacing.md` and `insets.bottom + spacing.md` — animates over the same duration as the keyboard slide. No discontinuity, dynamic value, small gap. (B) **`OnboardingSearch` dropped FlatList** in favor of mapped flex column. The three search-step screens now wrap their body content in a `ScrollView` (with `keyboardShouldPersistTaps="handled"` + `keyboardDismissMode="on-drag"`); the `searchWrap` flex-1 prison that couldn't fit a result row when keyboard was up is gone. Body content lays out at natural height, overflows scroll naturally. iOS's automatic keyboard-aware scroll behaviour (TextInputs inside ScrollView get scrolled into view on focus) is now in play. `SafeAreaView` back to `edges={['top']}`, footer paddingBottom inline + `useKeyboardOpen`.
- Auto-scroll onboarding search to keep result visible above continue button (`07b3bf3`). Even with the ScrollView wrap, when a single result row appeared after typing, its bottom edge was butted right against the Continue button's top edge with zero breathing room — read as "overlap" visually. (A) `OnboardingSearch` gets a new `onResultsRendered?: () => void` prop, fired from a `useEffect` watching the `results` state whenever it transitions to a non-empty list. (B) Each of the three search screens holds a `ScrollView` `ref`, registers a handler that calls `scrollRef.current?.scrollToEnd({ animated: true })`, and passes it as `onResultsRendered`. (C) `bodyContent.paddingBottom` bumped from `spacing.lg` (24) to `spacing.xxl` (48) so even after the auto-scroll lands at the content end, the last result row keeps comfortable visual separation from the Continue button.

**Bug fixes & lessons**
- React state updates from `setState` (even with `await` inside an async function) don't propagate to other consuming components instantly — the consumer re-renders on the next React render after the setState lands. If the same callback ALSO schedules a navigation (e.g. `router.replace('/(tabs)')`), the routing/segments change can outpace the state propagation: the consumer renders with new segments but old state, triggering whatever the layout effect does in that combination. **Decision rule**: if the post-mutation flow involves both updating shared state AND navigating, let the consuming layout's effect own the navigation based on the new state. Manually calling navigate alongside refresh is a race the layout will lose half the time.
- React Native flex children default to `minHeight: 'auto'` (= content height), which means a `flex: 1` column won't shrink below its children's natural height — children spill past the parent's bounds. The fix is `minHeight: 0` on each flex node in the chain. If a `FlatList` is inside a `flex: 1` container that's inside another `flex: 1` container, every level needs `minHeight: 0` or the innermost list will refuse to shrink and will overflow into whatever sibling sits below.
- `KeyboardAvoidingView`'s `keyboardVerticalOffset` documentation describes it as "distance from screen top to KAV top", NOT "subtract from bottom". So passing `insets.bottom` (the home-indicator inset) to cancel a SafeAreaView's bottom edge does NOT work as you'd hope — empirically the gap to the keyboard was inconsistent across screens. The reliable pattern when you want the footer hugged to the keyboard is: drop the SafeAreaView's bottom edge (`edges={['top']}`), let KAV pad with the default offset (0), and manually add `insets.bottom` to the footer's `paddingBottom` only when the keyboard is closed (use a `useKeyboardOpen` hook). Direct, no magic.
- `supabase-js`'s `auth.getUser()` (no-arg) does NOT use the global `Authorization` header set on `createClient`. It uses GoTrueClient's internal session, which is empty on a fresh server-side client. Pass the JWT explicitly: `auth.getUser(jwt)`. Worth remembering for any Edge Function that gates on user identity via a forwarded JWT.
- `useColorScheme()` in React Native can briefly return the "wrong" value during view transitions on iOS — `'dark'` for one frame before settling to `'light'`. When a prop like `keyboardAppearance` depends on the scheme, this manifests as a visible flash. If the app supports only one mode today (or strongly prefers one), hardcode the prop instead of deriving from the hook. The conditional `scheme === 'dark' ? 'dark' : 'light'` is technically correct but isn't worth the flash.
- `keyboardAppearance` prop on `TextInput` is read from the bridged native config at the moment iOS decides to show the keyboard, which can race the React Native bridge by a frame or two with `autoFocus`. Even with `keyboardAppearance="light"` hardcoded, you can still see a brief dark flash if the app's `userInterfaceStyle` (in `app.json`) is `"automatic"` and the system mode is dark. Locking `userInterfaceStyle: "light"` at the app config level fixes this at the iOS UI-traits level, but it's a **native build-time setting** — JS hot reload won't pick it up. Requires `npx expo prebuild --clean && npx expo run:ios` or a fresh EAS dev build to land on the device.
- `LogBox.ignoreLogs(...)` only suppresses the visible red dev toast. It does NOT intercept the actual error or the cleanup path supabase-js takes afterward. So `ignoreLogs(['Invalid Refresh Token'])` is safe — it doesn't mask any state machinery, just quiets a noisy `console.error`. Don't be afraid of it when the underlying error is recoverable and the noise is dev-only.
- supabase-js does NOT auto-recover when the user referenced by the access token has been deleted server-side. The access token validates structurally and decodes locally fine; only the proxy's `auth.getUser(jwt)` call detects "User from sub claim in JWT does not exist". Auto-refresh fires only on token expiry, and by then the refresh token is also dead (it's tied to the same user) — so the cached session sits forever. The fix is to detect the proxy's 401 client-side and call `supabase.auth.signOut()` explicitly to clear the bad session. Same pattern would apply to any Edge Function that gates on `auth.uid()`.
- React state changes that depend on a child component's internal state need an explicit callback — there's no idiomatic way for a parent ScrollView to know that a child's `useEffect`-driven re-render produced new content. Exposing a callback like `onResultsRendered` from the child (fired from its own `useEffect` watching the relevant state) keeps the API minimal without leaking the child's internal state shape upward. Cleaner than lifting the state itself.
- `LayoutAnimation.configureNext({ duration: e.duration })` called inside a `keyboardWillShow`/`Hide` handler animates ANY layout change in the next render — not just the keyboard's KAV padding — over the same duration. Useful when you have a state-driven layout property (like a dynamic `paddingBottom`) that you want to ride the keyboard's animation. Without this, the state change snaps instantly while the keyboard slides smoothly, which reads as a jump.

**Next**
- Configure the Supabase database webhook for `send-push-notification` (manual dashboard step; function is deployed but never fires until the webhook is wired).
- Finish the EAS iOS dev build interactively to pick up `expo-notifications` + `expo-haptics` (the `--non-interactive` attempt failed on the missing `aps-environment` provisioning profile).
- Three rec actions on title detail when arrived via `?fromRec=<id>` ("Already seen / Add to my list / Not for me").
- Refactor inline avatars to the shared `<Avatar />` component.
- Extract `<NotificationBell unreadCount />` shared between `ScreenHeader`, Library, and Home (overdue — third use site shipped).
- Optional: extract a shared `<StarsRow>` from `RatingSheet` so best-watched's inline rating can support drag too.
- Optional: add `items.watchlist_at` column for ordering watchlist by add-time rather than `updated_at`.
- Optional: if dark mode ever becomes a supported app variant, revisit the hardcoded `keyboardAppearance="light"` in onboarding TextInputs and replace with a stable scheme source (probably the `userInterfaceStyle` from `app.json` rather than `useColorScheme()` directly).

**Open questions**
- None.

---

## 2026-05-20

**Done**
- TMDB proxy body-decoder fix (`74a395a`). The Home dashboard was hitting `tmdb-proxy 500: Internal Server Error` on every open. Root cause: the proxy was calling `await tmdbResponse.text()` outside any inner try/catch, and the Supabase Edge runtime's HTTP body reader was throwing `"unexpected end of file"` mid-body on TMDB's compressed/chunked responses. The original `Internal Server Error` text was the Edge runtime's default 500 page surfacing the uncaught exception. Three layered changes to the proxy: (1) top-level try/catch around the whole handler so any unhandled exception returns a structured JSON 500 with the actual error in `detail` (so the client sees what failed instead of generic text); (2) inner try/catches around the outbound `fetch` (502 `upstream_fetch_failed`) and the body read (502 `upstream_body_read_failed`) for diagnostic granularity; (3) **`Accept-Encoding: identity`** on the outbound TMDB request — this is the actual fix, forcing TMDB to send a non-compressed non-chunked body so the runtime's decoder never gets out of sync. TMDB JSON is small enough that the bandwidth trade-off is negligible. Also added `console.error`/`console.warn` lines so the dashboard logs surface the path + status + detail on every failure. Verified on device: warnings stopped.
- Home activity tolerance (`3209c28`). Separately from the proxy fix, hardened the Home dashboard against single-TMDB-call failures: the activity item's metadata fetch was the only unwrapped `await` in `fetchHomeData` (friend cards and recs were already using `Promise.allSettled`). Wrapped it in `.catch(() => null)` — on failure the section just doesn't render, rather than the whole Home crashing. Defence-in-depth alongside the proxy fix; even if a future TMDB failure slips past the proxy hardening, the screen still loads.
- Library tab refactor to Spotify-style header (`df44dda`). Removed the inline TMDB search that used to take over the body of the Library screen. **New header** is a custom layout (not `ScreenHeader` — the three-column centred-title shape doesn't fit Spotify's left-aligned title + right-aligned icon row). Two modes: normal (left-aligned `typography.display` "Library" + Search/Plus/Bell icons on the right, `gap: spacing.base`) and search-active (full-width `TextInput autoFocus` + Cancel button in `palette.accent`). State `searching: boolean` flips between them; `exitSearch()` clears the filter and dismisses the keyboard. Bell badge still wired to `useUnreadCount` — bell-with-badge JSX is ~25 lines duplicated from `ScreenHeader`; held off on extracting a `NotificationBell` component until a third site needs it (same threshold I held off on for `<Avatar />`). **Local filter** is client-side `rows.filter(r => r.title.toLowerCase().includes(...))` over the already-loaded library — no new fetch. Empty-state copy switches to `'No matches for "X"'` when the filter is non-empty. **New modal** at `src/app/library/add.tsx` reachable via the Plus icon: resurrects the old standalone Search experience as a pushed modal with X-close header, `autoFocus` TextInput, 300ms-debounced TMDB results. Tapping a result pushes the existing `/title/[mediaType]/[tmdbId]` modal on top; when the user dismisses it they land back on the add-search modal with state preserved (because the modal's `useState` survives the cover-and-uncover cycle). Modal route registered in root `_layout.tsx` alongside the existing title-detail and recommend modal registrations.
- Rating sheet: thumbs → 5 stars (`21922c0`). Replaced the thumbs-up / Skip / thumbs-down row on the post-watched rating sheet with a 5-star row + Skip-below layout (lucide `Star`). `items.rating` now stores the raw 1-5 value directly instead of being hardcoded to 2 (down) / 4 (up). The credibility signal between friends — `recommendations.rating_thumb` — is still up/down but is now *derived* from the star value: 1-2 → down, 3-5 → up. `handleRate` signature changed from `(thumb: 'up' | 'down' | null)` to `(rating: number | null)`; skip path is unchanged (no `items.rating` write, no `rating_thumb` write, but recs still transition to `watched`). New `StarRow` local helper with press-in/press-out fill preview driven by a `pressedRating` state — pressing the 4th star fills stars 1-4 with `palette.accent` before commit fires on release; `palette.textMuted` is the outline colour for unfilled stars. Tap targets are 44×44 with `hitSlop: spacing.xs` for the recommended thumb-friendly minimum. Re-rate flow (re-tap Watched while already watched) still works — `pressedRating` starts `null` each open so there's no pre-selection, matching the spec.
- Home redesign + rec attribution across Library and title detail (`8ead913`, +1047/−588 across 5 files). Three logical changes shipped in one commit because they share extracted scaffolding. (A) **Home tab rewrite** (`src/app/(tabs)/index.tsx`): swapped `ScreenHeader` for the same custom header pattern Library uses (left "Seen" + right Mail icon w/ badge — Mail not Bell, per spec); a Pressable styled as a search input below the header pushes `/library/add` (no input handling on Home itself); three new sections — **Recs for you** (horizontal 120×180 poster cards, pending+accepted recs received from friends, sender avatar + "From {firstName}" attribution, tap → title detail with `?fromRec=<id>`), **Friends are watching** (horizontal 100×150 cards, attribution now varies by friend's status — "{name} is watching this" vs "{name} watched this"), and **Currently watching** (vertical list of user's `status='watching'` items with an inline "Mark watched" pill on each row — tap pill transitions `items.status='watched'` then opens the rating sheet; tap row body → title detail). Replaced the old activity-card/recent-recommendations sections entirely. (B) **Library rec attribution** (`src/app/(tabs)/library.tsx`): each row now shows a "Rec'd by {names}" subtitle when one or more friends recommended that title (status IN ('accepted','watched')). Format: "Rec'd by Sarah" / "Rec'd by Sarah & Tom" / "Rec'd by Sarah +2 others". Single batched query joins all recs to me, grouped by `${media_type}:${tmdb_id}` so per-row lookup is O(1) — chose this over per-row queries (N+1) or per-tab refetch (more bandwidth across tab switches). (C) **Title detail always-on attribution** (`src/app/title/[mediaType]/[tmdbId].tsx`): the existing fromRec-specific card now shows whenever anyone recommended this title to me (not just when arrived via `?fromRec`); `RecContext` shape changed from `{ sender, note }` to `{ senders: Sender[], totalCount, note }`. `note` is loaded separately and only when `fromRec` is in URL (pins which rec's note to surface). Sender line formatted by the same N-name pattern. (D) **Extracted scaffolding** to support reuse between Home's new Mark-watched flow and title detail's existing flow: `src/components/rating-sheet.tsx` (the bottom-sheet UI, caller owns visible/busy state, component owns press-fill preview) and `src/lib/rating.ts` (`thumbFromRating(1-5)` + `applyWatchedRating({userId,tmdbId,mediaType,rating})` — moved out of title detail since Home now also writes ratings). Title detail's inline Modal/StarRow + ~60 lines of sheet styles were deleted in favour of the shared component. `useUnreadCount` is unchanged and still works (verified — Home's Mail badge and Library's Bell badge both still drive off it).
- Inline TMDB search on Home — first pass (`f420f34`). Replaced the fake-input Pressable with a real `TextInput` on the Home header. Initial design used a React Native `Modal` (animationType="slide", transparent, `presentationStyle="overFullScreen"`) as the results sheet, with a transparent top scrim sized via `insets.top + 110` to position the sheet just below the input. 300ms debounce + active-flag stale guard mirroring `src/app/library/add.tsx`. Empty states standardised at the same time: all three Home empty CTAs converted from separate `<Pressable>` buttons to inline `<Text onPress suppressHighlighting>` accent links nested inside the body Text — reads as one flowing paragraph per the spec. Trade-off noted: inline links don't support `hitSlop`, so the tap target is the rendered text bounds (~16pt × 22pt line-height), below the 44pt recommended minimum. The user accepted the trade-off for paragraph cohesion.
- Inline search switched to absolutely-positioned overlay after Modal focus conflict (`c7e0559`). The Modal-based design from `f420f34` had a critical bug: tapping the `TextInput` triggered `onFocus` → `setSearchOpen(true)` → Modal mounted → Modal stole focus from the input. The keyboard never appeared and typing was impossible. Diagnosed by inserting four `console.log` probes (focus / text change / debounce fire / results count) and asking the user to paste Metro output — the `focus fired` log didn't fire, confirming the Modal was intercepting the tap before the input gained focus. Refactored: dropped the `Modal` import entirely, replaced with a conditionally-rendered absolutely-positioned `<View>` (`position: 'absolute'`, `top: searchSheetTop`, `left/right/bottom: 0`, `zIndex: 10`, hairline `borderTop`). The header + search bar stay in normal flow at the top, undisturbed and tappable; the overlay only covers the area below the input. Bumped `SEARCH_SHEET_TOP_OFFSET` from 110 to 116 (more accurate calc: header padding 24 + display lineHeight 38 + searchBar marginTop 8 + searchBar height 44 = 114, +2 for breathing room). Dismiss is X-only — the original spec's "tap area above the View" requirement was dropped because implementing it requires layering a Pressable scrim behind the input in z-order, which is the exact category of trap we just dug out of. Logs removed after the user confirmed the fix.
- Standardised friend-invitation paths (`4e77d3a`). Two related changes to make `/friends/add` the single landing page for both real-friend handles and not-yet-on-Seen friends. Home's Recs-for-you empty state CTA now reads "Add friends" → `/friends/add` (was "Invite friends" → `/friends/invite`, which split the user's mental model). Friends-watching empty state was already on `/friends/add`. The `/friends/add` screen itself gained a section below the handle form: hairline "Or" divider (uppercased, 1-letter-spaced `typography.caption`/`palette.textMuted`) then a row with "Friend not on Seen yet?" + `Send invite link` button in `palette.accent` routing to `/friends/invite`. Both invitation paths now reachable from one screen.
- Friends-watching empty-state orphan fix (`4067c35`). One-character tweak: changed `{' '}` to `{'\n'}` between "See what your friends are watching." and the inline "Add friends" link so the link reads on its own line. Keeps the inline `<Text onPress>` styling — just forces the linebreak.
- Push notifications wired end-to-end (`297ac30` + `d9b81b9`). Three pieces: (A) **client registration** in `src/lib/push.ts` — `registerForPushNotifications()` returns the Expo push token *only if* permission is already granted (never prompts); `requestPushPermission()` triggers the system prompt; `savePushToken(token, userId)` upserts into `push_tokens` with `onConflict: 'user_id,device_id'`. The `device_id` is a per-install UUID persisted in AsyncStorage (`seen.push.device_id` key) — uninstall + reinstall generates a new one. **`maybeEnablePushAfterAccept(userId)`** is the orchestrator: checks permission status, shows an in-app `Alert` explainer if `undetermined`, then if user confirms calls request → register → save. Silent on every failure branch. (B) **Permission flow integrated** into both `handleAccept` paths in `friends/requests.tsx` and `inbox.tsx` — the friend-accept tap is the "social commitment moment" where it's natural to ask for notifications. (C) **Edge Function** at `supabase/functions/send-push-notification/index.ts` — database webhook handler on `notifications.INSERT`. Fetches the recipient's tokens via service-role, resolves names (profiles) and titles (TMDB direct call with the same `Accept-Encoding: identity` defence as tmdb-proxy), fans out one message per device to `exp.host/--/api/v2/push/send`, inspects tickets for `DeviceNotRegistered` and reaps stale tokens. Initial design (`297ac30`) had a custom `WEBHOOK_SECRET` header check; redeploy `d9b81b9` dropped that because Supabase's webhook UI auto-populates `Authorization` with the anon key (the default Edge Function gateway already verifies that JWT), so a second secret-check was both redundant and configuration-friction. Per-kind payload copy: `{sender} recommended {title}` (note as body) / `{watcher} watched {title}` ("Your rec hit") / `{sender} wants to be your friend` / `{accepter} accepted your request`.
- RatingSheet improvements (`2c208d1`): haptics + explicit Done button + closer stars + pre-fill on re-rate. Four logical changes shipped in one commit. (A) `Haptics.impactAsync(Light)` on each star press-in — Letterboxd-style tactile feel, fire-and-forget so unsupported devices stay silent. (B) Replaced auto-commit-on-tap with a tentative `selected` state and a filled-pill Done button below the stars; tapping the same star a second time deselects (back to null); Skip preserved verbatim. (C) Star row `gap: spacing.sm` → `spacing.xs` (8px → 4px) for a tighter visual cluster. (D) New `initialRating: number | null` prop — `useEffect` on `[visible, initialRating]` resyncs the internal `selected` state each time the sheet opens, so re-rates land on the user's previous pick. Title detail tracks `currentRating` from the items query and updates after each successful `applyWatchedRating`; Home's `WatchingItem` gains `rating` and threads it through `ratingTarget`. Installed `expo-haptics` (native module — landed in the same in-flight EAS rebuild as `expo-notifications`).
- Drag-to-rate gesture on the star row (`7f0a23f`). PanResponder added to the row container with `onMoveShouldSetPanResponder` returning true above a 5px threshold — quick taps still flow to the per-star `Pressable`s, but movement claims the responder for drag selection. Coords: `onLayout` + `measure()` capture the row's `pageX` and width on mount; gesture's `moveX` minus `pageX` gives row-relative X; `valueFromRowX(x, width)` maps to 1–5 with clamping at both ends. Drag haptics fire **once per new star** via `lastHapticStarRef` (shared between the Pressable's onPressIn and the PanResponder so transitioning between them doesn't double-haptic). Drag release commits with **set** semantics (no toggle — dragging to "3" means "I want 3"); tap release keeps the toggle semantics. Required swapping `justifyContent: 'center'` on `starsRow` for `alignSelf: 'center'` so `onLayout` reports just-the-stars width — the gesture math needs that.
- Onboarding flow end-to-end (`8d4ed0b`, +1909 across 14 files). Six-screen flow gated by a new `profiles.onboarded boolean NOT NULL DEFAULT false` column (migration `20260520120000`, existing test users backfilled to `true`; rls-auditor PASS — column-only change, RLS posture unchanged, signup trigger picks up the default). Screens in order: **welcome** (no back), **handle** (validates against `^[a-z0-9_]{3,20}$` + profanity, surfaces unique-constraint 23505 as "Handle taken"), **display-name** (random-name seed, dice button to reroll, profanity check, 30-char limit), **last-watched** (`OnboardingSearch` + items upsert as status=watched), **best-watched** (search + inline tap-only star rating; persists items.rating immediately on tap), **watchlist** (3-item picker with `pickedKeys` dimming + "1 of 3 added" counter). `OnboardingSearch` (`src/components/onboarding-search.tsx`) is the shared TMDB search input + debounced results list used by screens 4-6 — owns its own state, auto-clears the query on pick. `OnboardingDots` (`src/components/onboarding-dots.tsx`) is a tiny 6-dot progress indicator pinned to the bottom of each screen. **Routing logic** in `src/app/_layout.tsx` was restructured: `<ProfileProvider>` from the new `src/hooks/use-profile.tsx` wraps `<RootLayoutInner>`, the inner reads `useSession()` + `useProfile()` and routes between `(auth)` / `(onboarding)` / `(tabs)`. `useProfile` is Context-based (not a plain hook) so onboarding screens can call `refresh()` after writing `onboarded: true` and the root layout sees the new state on the very next render — avoids a race where `router.replace('/(tabs)')` would otherwise bounce back to `/(onboarding)/welcome`. Shared `finishOnboarding({ onComplete, refreshProfile })` helper in `src/lib/onboarding-utils.ts` (alongside `RANDOM_NAMES`, `PROFANITY_WORDS`, `randomDisplayName`, `containsProfanity`, `validateHandle`) — Skip on steps 4-6 and Continue on step 6 all route through it. `database.types.ts` regenerated via `supabase gen types typescript --linked`. Trade-offs noted at build time: (i) `items.watchlist_at` doesn't exist; watchlist step just sets `status='watchlist'` and relies on the generic `updated_at` trigger; (ii) inline rating in best-watched is tap-only (no drag) since `RatingSheet` is `Modal`-based and not inline-friendly — drag stays exclusive to the sheet; (iii) `finishOnboarding` takes an `onComplete: () => void` callback rather than the typed router, keeping the helper outside Expo Router's typed-route generic surface.
- Onboarding step 6: watchlist → currently watching (`f4769e1`). Replaced the 3-item watchlist setup with a single-item "What are you watching right now?" prompt, writing `status='watching'` instead of `'watchlist'`. Body copy: "Anything you're in the middle of? Add it — we'll keep track." File renamed `watchlist.tsx` → `currently-watching.tsx` (git tracked as 60% similarity rename), with `best-watched.tsx`'s push target updated to match. State simplified from `picked: PickedItem[]` to `added: AddedItem | null`; Continue enables once one item is added; repeated picks overwrite via the existing `(user_id, tmdb_id, media_type)` unique constraint. Mirrors the structural pattern of `last-watched.tsx` (search + below-the-search confirmation card). Dots still `currentStep={6}` since this is still the final step.
- TMDB auth race fix + profanity filter expansion (`9864bd0`). **TMDB**: `callProxy` in `src/lib/tmdb.ts` now reads `supabase.auth.getSession()` and attaches `Authorization: Bearer <access_token>` explicitly via the `headers` option to `functions.invoke`, rather than relying on supabase-js v2's event-driven JWT propagation. The internal listener that hands the user JWT from `auth` to `FunctionsClient` is racy just after `signInWithIdToken` — the FunctionsClient's cached `Authorization` was still the anon key when the onboarding LastWatched screen made its first search, so the proxy's `auth.getUser()` call returned no user and a 401. Explicit headers close the race for every call site (not just onboarding). File header rewritten to document the why. **Profanity**: expanded `PROFANITY_WORDS` from 10 slurs to 22 entries (added `fuck shit cunt dick pussy asshole bitch bastard cock twat wanker piss`); switched `containsProfanity` from substring `.includes` to a regex with `\b...\b` word boundaries — `shit` blocks `shit` but lets `shitake` through (resolves the Scunthorpe problem). File comment rewritten to reflect the new scope and matching strategy.

**Bug fixes & lessons**
- Edge functions that throw an uncaught exception return Edge runtime's plain `"Internal Server Error"` text — not our structured JSON. Wrapping the entire handler in try/catch and returning the actual exception message in the response body turned an opaque "something failed" into a diagnosable `"unexpected end of file"` within one reload. Lesson applies to any Edge function we write later.
- The Supabase Edge runtime + chunked/compressed upstream responses is a known interaction-bug class. `Accept-Encoding: identity` on outbound `fetch` is the safe default for small JSON APIs.
- React Native's `Modal` always steals focus from any underlying input — even with `transparent` and `presentationStyle="overFullScreen"`. If you need an overlay that doesn't capture taps for an input that's already on the screen, use an absolutely-positioned `<View>`, not `Modal`. Modal is the wrong primitive when the user expects to keep typing into an input that's not inside the modal. Decision rule: if the keyboard needs to stay attached to a TextInput outside the overlay, use absolute positioning; if the overlay owns the TextInput, Modal is fine.
- Diagnosing focus/typing issues: insert numbered `console.log` probes at every event boundary (focus, change, effect, network) and ask the user to paste Metro output. Which log doesn't fire localises the failure faster than any amount of reasoning from the code alone. Worked twice this session — once for the TMDB proxy chunked-encoding error, once for the Modal focus conflict.
- Supabase database webhooks auto-fill `Authorization: Bearer <anon key>` and the default Edge Function gateway already verifies that JWT. Adding a custom `WEBHOOK_SECRET` check on top is redundant in the dashboard UI flow (the user would have to manually override the auto-filled header). For Edge Functions backing internal Supabase webhooks, lean on the gateway's anon-key check at MVP scale; add a shared secret only when you need to lock the function down even against leaked anon keys.
- Adding a new native capability (e.g. push notifications via `expo-notifications`) after the initial EAS dev build invalidates the cached provisioning profile — Apple needs to issue a new one with the right entitlement (here, `aps-environment`). `eas build --non-interactive` *cannot* do this regeneration because it requires Apple ID auth, which is a 2FA-gated interactive prompt. Hit this with the first push-notification dev build: build failed with "Provisioning profile doesn't support the Push Notifications capability." Fix is to run `eas credentials --platform ios` (interactive) to remove and regenerate the profile, or just re-run the build without `--non-interactive`. Worth remembering for any future native-capability addition (background fetch, in-app purchases, etc.).
- TypeScript module-rename gotcha: `use-profile.ts` returning `<ProfileContext.Provider>` JSX produces a wall of "TS1005: '>' expected" errors. Fix is to rename `.ts` → `.tsx` so the compiler parses the file as JSX. Worth checking when a hook starts wrapping children in a provider mid-refactor.
- `npx supabase gen types typescript --linked 2>&1 > database.types.ts` clobbers the file — the CLI's "Initialising login role…" status messages go to stderr but our `2>&1` merges them into the redirected stdout. Use `2>/dev/null > database.types.ts` (drop stderr) or `2>>/tmp/log > database.types.ts` (preserve stderr separately).
- supabase-js v2's `functions.invoke` JWT auto-attach is event-driven and racy right after `signInWithIdToken`. The `FunctionsClient` caches its `Authorization` header at construction (anon key) and updates it only when an internal `_handleTokenChanged` listener fires for `SIGNED_IN` / `TOKEN_REFRESHED`. If a function call lands between the sign-in completing and that listener propagating the new token, the FunctionsClient sends the anon key — which fails any Edge Function that calls `auth.getUser()` to validate the caller. **Lesson**: for Edge Functions that gate on user identity, always read the session and attach `Authorization: Bearer <access_token>` explicitly via `functions.invoke({ headers })`. Same call should still work if there's no session (header is `undefined`, function returns 401 — correct). One-line defence at the call site is cheaper than chasing flaky 401s.
- Substring profanity matching has the Scunthorpe problem out of the box: `'shit'` blocks `'shitake'`, `'piss'` blocks `'narcissist'`. Whole-word matching via `new RegExp('\\b(' + words.join('|') + ')\\b', 'i')` solves it. The regex is rebuilt per call, but for handle/display-name validation that fires on every keystroke the cost is below noise.

**Next**
- Three rec actions on the title detail screen when arrived via `?fromRec=<id>` — "Already seen / Add to my list / Not for me" — wired to `recommendations` status transitions. Still carrying over.
- Refactor inline avatars in `profile.tsx`, `(tabs)/friends.tsx`, `friends/requests.tsx` to use the shared `<Avatar />` component.
- Extract `<NotificationBell unreadCount />` shared between `ScreenHeader`, the Library tab's custom header, and the Home tab's custom header — overdue (third use site shipped on 2026-05-20).
- **Configure the database webhook in Supabase dashboard** (manual step — `Database → Webhooks → Create` on `notifications.INSERT` pointing at `https://xhzrsdgrgimlrdnyzidr.supabase.co/functions/v1/send-push-notification`). Until this exists, the deployed function never fires.
- **Finish the EAS iOS dev build interactively** to pick up `expo-notifications` + `expo-haptics` (the `--non-interactive` attempt failed on the missing `aps-environment` provisioning profile; needs `eas build --profile development --platform ios` without the flag so Apple 2FA can prompt).
- Optional: extract a shared `<StarsRow>` from `RatingSheet` so the inline best-watched rating can support drag too. Right now drag lives only in the modal sheet.
- Optional: add an `items.watchlist_at` column if we ever want to order watchlist by when it was added rather than `updated_at`.

**Open questions**
- None.

---

## 2026-05-19

**Done**
- Added temporary sign-out button to the home placeholder (`src/app/index.tsx`). Pressable styled with `palette.accent` + `typography.body`, wrapped in `spacing.base` padding with extra `hitSlop`. Marked `// TEMP: will move to profile/settings screen in PRD §4` so it's findable when the real profile tab lands. Sign-out propagates via the `onAuthStateChange` subscription in `useSession` → the root layout's redirect `useEffect` routes back to `/(auth)/sign-in` automatically. Committed `fd1db46`.
- Built the TMDB proxy Edge Function. Scaffolded with `supabase functions new tmdb-proxy`, then rewrote the scaffold's `withSupabase` pattern with explicit `Deno.serve` + `createClient` (the scaffold's wrapper validates the apikey, not the user JWT — TECHNICAL §3 requires per-user auth, so the explicit pattern matches the spec). Security model: caller's `Authorization` Bearer JWT is verified via `supabaseClient.auth.getUser()` (anything else → 401), `path` query param runs through `^[a-zA-Z0-9/_-]+$` character filter, then a 4-pattern allowlist (`search/(movie|tv|multi)`, `movie/\d+`, `tv/\d+`, `configuration`). Forwards remaining params to TMDB with the read-access token from `TMDB_ACCESS_TOKEN` Supabase secret. Deployed as version 1, then version 2 after a fix: `supabase.functions.invoke` sends params as JSON body (POST), so the function now reads from both URL query string (curl/debug) and POST body. Curl smoke test with the anon key returns 401 from our handler (correct — anon key isn't a user JWT). Committed `d654aa1`.
- Built the typed TMDB client wrapper `src/lib/tmdb.ts`. Six public functions matching the proxy allowlist: `searchMulti`, `searchMovies`, `searchTV`, `getMovie`, `getTV`, `getConfiguration` — all return typed Promises via `supabase.functions.invoke<T>('tmdb-proxy', { body: { path, ...params } })`. Minimal type shapes (only fields the app currently reads). `TMDBMediaItem` is a discriminated union (`movie | tv | person` keyed by `media_type`) for `search/multi`; the single-type searches return un-discriminated arrays. `imageUrl(path, size)` builds direct TMDB CDN URLs (`https://image.tmdb.org/t/p/` — image fetches do not go through the proxy per TECHNICAL §3). Error handler unwraps `FunctionsHttpError.context` to surface the proxy's response body. Committed `7a05bcd`.
- `tsconfig.json` excludes `supabase/functions/**` from the app's tsc — that's Deno code on a different runtime (uses `https://esm.sh` URL imports and `Deno.serve`/`Deno.env` globals), and the project's Node-flavoured tsc can't type-check it. The Deno VS Code extension (config from the scaffolder) handles it locally; the Edge runtime checks it on deploy.
- EAS dev build configuration committed (`4d2c052`): `eas.json`, `expo-dev-client@~6.0.21`, `app.json` gained `extra.eas.projectId` and `ios.infoPlist.ITSAppUsesNonExemptEncryption: false` (App Store export-compliance flag EAS sets for apps using `signInWithIdToken`). These were generated by the `eas build:configure` and first dev-build runs from the prior session — committing now in a single self-contained commit.
- Process note: when `supabase functions deploy` runs without Docker, it still deploys via the Management API but warns. We've been relying on this workaround throughout (Docker isn't running in this dev environment). Worth knowing — if a future feature needs local Edge function testing via `supabase functions serve`, we'll need Docker.
- Secrets state: `TMDB_ACCESS_TOKEN` is set as a Supabase secret on `xhzrsdgrgimlrdnyzidr`. Hook-blocked from Claude reads.
- End-to-end TMDB pipeline verified on device. Added a temporary `useEffect` in `src/app/index.tsx` calling `searchMulti('severance', 1)` and logging `TMDB OK: <n> results` or `TMDB FAILED: <err>` on mount, reloaded the dev build on the iPhone, observed the success log in Metro, then removed the smoke test. The full chain — app session token → `supabase.functions.invoke('tmdb-proxy', { body })` → Edge function user-JWT verification → TMDB v4 call with the secret token → typed response back — works as designed. Smoke test was never committed (lived only in the working tree between add/remove).
- Tab navigator scaffolded with Search as the first real screen (`df94d3c`). Five tabs under the `(tabs)` group — `index` (Home), `library`, `search`, `friends`, `profile` — wired to lucide-react-native icons (`Home`, `BookMarked`, `Search`, `Users`, `UserCircle`). `tabBarActiveTintColor: palette.accent`, inactive `palette.textMuted`, bar styled with `palette.bg`/`palette.border`. `headerShown: false` everywhere; each screen handles its own surface. The previous home placeholder (Seen wordmark + sign-out) moved cleanly into `(tabs)/index.tsx` via `git mv` (98% similarity). The other three tabs are minimal "Coming soon" stubs (`library.tsx`, `friends.tsx`, `profile.tsx`). All tab screens use `SafeAreaView edges={['top']}` so they don't double-pad against the tab bar's own bottom inset.
- Real Search screen at `(tabs)/search.tsx`: TextInput at the top (autoCapitalize off, autoCorrect off, returnKeyType=search) → 300 ms debounced `searchMulti` call → FlatList of results. Stale-response guard (`active` flag in the effect) so a slow earlier request can't overwrite a newer one. Filters out `media_type: 'person'` via a typed type-guard so the renderer sees only the `movie | tv` half of `TMDBMediaItem`. Each row: 56×84 poster via `expo-image` with `transition={150}` (placeholder block in `palette.surfaceAlt` when `poster_path` is null), title + `YEAR · Movie|TV Show` meta line. Four states: empty (`Search for a film or TV show`), loading (centered `ActivityIndicator` in `palette.accent`), no results (`No results for "<query>"`), error (the thrown message from `tmdb.ts`'s `FunctionsHttpError` unwrap).
- Root `_layout.tsx` redirect target moved from `/` to `/(tabs)` — both resolve to the same screen via group transparency, but the explicit form makes the intent obvious in the diff.
- Installed `lucide-react-native` (pure-JS, no native deps, via `npx expo install`).
- Search-screen UX fix (`c8466e2`): wrapped everything below the TextInput in a `<Pressable onPress={Keyboard.dismiss}>` so tapping outside the input dismisses the keyboard. No visual treatment on the Pressable (`{ flex: 1 }` only). `keyboardShouldPersistTaps="handled"` on the FlatList is preserved, so once rows gain their own `onPress` later, the child handler will win and the wrapper won't swallow row taps.
- Title detail modal at `src/app/title/[mediaType]/[tmdbId].tsx` (`f96b002`). Modal presentation configured via `<Stack.Screen name="title/[mediaType]/[tmdbId]" options={{ presentation: 'modal' }} />` in the root layout — iOS gets the sheet + swipe-to-dismiss for free. Detail fetched in parallel with the items lookup for current library status (`useEffect` with stale-guard). UI: 240px backdrop, poster overlapping via negative `marginTop`, display-typography title, year + runtime/seasons meta, italic tagline, surfaceAlt genre pills, full overview, then three Pressable action buttons (Watchlist / Watching / Watched). Active status fills with `palette.accent`, inactive stays outline. Upsert uses `onConflict: 'user_id,tmdb_id,media_type'` matching the unique constraint from migration 3; `watched_at` only set on the transition TO `watched` (column-omit semantics preserve existing values). Floating round close button at `insets.top + sm`, `palette.overlay` chip background. Search rows wired up with `router.push(\`/title/${media_type}/${id}\`)` — child Pressable wins over the keyboard-dismiss wrapper per React Native event bubbling.
- Two follow-up patches on the title screen (`91d8264`):
  1. Error surfacing on the status upsert. Previous catch returned a generic `'Unknown error'`. Now logs the full error object to `console.error` first (so Metro carries every Supabase `PostgrestError` field — `code`, `details`, `hint`, `message`), then formats the Alert with `message` + `hint` when present, `String(err)` fallback.
  2. `LinearGradient` swapped for a plain `<View>` styled `{ backgroundColor: palette.bg, opacity: 0.7 }` until the next EAS dev build picks up the native module. TEMP comment in the JSX has the verbatim restore line so future-me can drop the gradient back in without re-discovering the API.
- **Real bug found — RLS without grants.** The title screen's status update was failing with Postgres `42501 permission denied for table items`. Root cause: our six schema migrations enabled RLS and wrote policies on every table but never granted the underlying table privileges to the `authenticated` role. Postgres applies grants AND RLS as two separate layers — without grants, every client mutation fails before RLS ever evaluates. The rls-auditor missed this across six migrations because its checklist only covered RLS, not grants.
- Backfill migration `supabase/migrations/20260519102336_grant_authenticated_privileges_on_public_tables.sql` (`28fed53`). Grants `usage on schema public` plus per-table privileges scoped exactly to the operations TECHNICAL §2 documents as client-allowed (`profiles: select, update`; `items, push_tokens: full CRUD`; `friendships: select only` — mutations go through SECURITY DEFINER RPCs; `recommendations: select, insert, update` — no client DELETE per §2; etc.). `handle_history` deliberately gets nothing (service-role only). Explicit `grant execute` on the six client RPCs plus the two policy helpers (`is_friend_of_auth`, `can_send_friend_request` — they're called from RLS expressions which run in the caller's context, so `authenticated` needs EXECUTE even though both are SECURITY DEFINER). Trigger functions enumerated by name in a comment as "intentionally NOT granted". No sequence grants — every PK is uuid (`gen_random_uuid()`), no serial/identity columns exist. Verified live via `has_table_privilege('authenticated', 'items', 'INSERT') = true` plus the corresponding negatives for tables/operations that should remain denied. Auditor PASS.
- TECHNICAL §2 gained a "Two-layer permission model" preamble (`34beb99`) — explains that Postgres applies grants then RLS, names the `42501` error code, and links to the backfill migration filename. Future-me hits this paragraph before walking the policy list.
- rls-auditor checklist patched (`da6de1b`) — added a "Grants match policies" check inside step 3 with five sub-bullets covering the policy-grant cross-check, the no-grant-without-policy code smell, the service-role-only exception (`handle_history`), the policy-helper EXECUTE wrinkle, and the prior-migration grep. New line in the per-table output template. The checklist will now FAIL any migration that adds RLS policies without matching grants.
- Auditor flagged two non-blocking follow-ups for a future migration if desired: (1) `REVOKE EXECUTE ... FROM PUBLIC` on the six trigger functions to close a narrow surface where SECURITY DEFINER trigger functions are technically callable via `supabase.rpc()` (they'd fail without NEW/OLD context but `notify_*` and `handle_new_user` would attempt privileged inserts if the caller could coerce a NEW record); (2) document `generate_invite_token` in TECHNICAL §3 as a client-callable function for invite-token regeneration UX.
- Real Library screen replacing the "Coming soon" stub (`6c297c9`). Three sub-tab pills at the top (Watchlist / Watching / Watched), same filled-accent-vs-outline style as the title detail action buttons. Data flow: `useFocusEffect` with `[activeTab]` dep refetches `items` filtered by status, ordered by `updated_at desc`. For each item, a parallel `Promise.allSettled` of `fetchItemMeta(tmdb_id, media_type)` calls TMDB through the proxy; one failure surfaces as "Unable to load title" on that row without breaking the rest. Watched-tab rows get an extra `4★ · <date>` meta line when rating or watched_at are present (`.toLocaleDateString()` for now — will swap to PRD §7's relative/absolute formatter later). Stale-guard via `active` flag identical to Search. RLS does the user-scoping; query has no explicit `eq('user_id', ...)`. N+1 fetch is intentional for MVP scale; a comment block lists three concrete migration paths when 100+ item libraries surface (denormalise into items, AsyncStorage TTL cache, or TMDB `find` endpoint). One known unknown to verify on device: whether dismissing the title modal re-fires `useFocusEffect` on the Library tab below it — if not, swap to a Supabase realtime subscription on `items` instead.
- Search screen filters out poster-less results (`8ceb789`). `SearchableItem` type intersected each variant with `{ poster_path: string }` so the narrowed type forbids null; filter predicate extended with `&& !!item.poster_path`; renderRow's `item.poster_path ? <Image> : <View placeholder>` ternary collapsed to just `<Image>` since the type now guarantees a poster. Title detail screen left untouched — its `TMDBMovie`/`TMDBTV` types keep `poster_path: string | null` and the defensive fallback `<View>` stays as is.
- Verified full library loop end-to-end on device: TMDB search → tap result → title detail modal → tap status button → upsert via `items` → close modal → return to Library tab → row appears in the right status tab on refocus. The full Foundation-phase core loop is now solo-usable per PRD §9.1.
- Real Profile tab replacing the "Coming soon" stub (`774efef`). Card at top: 96px avatar circle (coral `palette.accent` background with the uppercase first letter of `display_name` in `palette.textInverse` when `avatar_url` is null, otherwise an `expo-image` of the URL), `display_name` in `typography.display`, `@handle` in muted body. Settings list below with three rows (Edit profile / Account / Sign out), flush against `palette.bg` per DESIGN's minimal-elevation guidance, separated by `StyleSheet.hairlineWidth` lines. Each row has a lucide `ChevronRight` chevron on the right. Sign out uses `Alert.alert('Sign out of Seen?', …)` with `destructive` style on the confirm button; on confirm, `signOut()` runs and the `useSession` → root-layout redirect handles the navigation. Two new placeholder routes at `src/app/profile/edit.tsx` and `src/app/profile/account.tsx`, each a SafeAreaView with a manual header (back chevron + title) and a centered "Coming soon" body. Removed the temporary `Sign out` button from `(tabs)/index.tsx` now that Profile owns the real one — home is back to just the centred "Seen" wordmark.
- Native-module-missing chain found on the placeholder screens: the back chevron rendered as a coral rectangle with "Uni…" text. Root cause: `react-native-svg` (transitive dep of `lucide-react-native`) is a native module that wasn't in the running EAS dev build — so every lucide icon in the app was rendering as React Native's "Unimplemented Component" placeholder. At icon-tab and `ChevronRight` sizes the placeholder is small enough to read as a colored block; at the `size=28` back chevron the truncated "Unimplemented" text became visible. Three-step fix:
  1. **Immediate workaround** (`2eac4a1`): swapped the back chevron in both placeholder screens for a text-based `‹ Back` Pressable in `palette.accent` + `typography.bodyEmphasis`, with TEMP comments carrying the verbatim restore line. Keeps the back button functional on the existing dev build.
  2. **Explicit dep pin** (`c5d1aa1`): `npx expo install react-native-svg` — SDK-54-compatible version `15.12.1` now pinned in `package.json` instead of living as a silent transitive dep. Future framework upgrades won't drop it accidentally.
  3. **EAS rebuild**: ran `eas build --profile development --platform ios --non-interactive`. Credentials reused (no Apple prompt), build succeeded (~10 min). Build URL: `https://expo.dev/accounts/trrblplsy/projects/seen/builds/701855a2-0c60-4b0f-924c-0e5c2b7790a0`. After installing on the iPhone, the user confirmed tab bar icons and the title-modal `X` now render as real SVG glyphs.
- Three restorations after the rebuild (`7eb7e60`): re-imported `LinearGradient` in the title detail screen and swapped the `<View opacity:0.7>` placeholder back to `<LinearGradient colors={['transparent', palette.bg]} />`; re-imported `ChevronLeft` in both placeholder screens and swapped the `‹ Back` text back to the icon. TEMP comments removed; `tsc --noEmit` clean. Net −10 lines.
- Rating prompt on the title detail's Watched action (`68b161c`). Tapping Watched still upserts `items.status = 'watched'` + `watched_at = now()` immediately; afterwards a bottom-sheet `Modal` slides up asking "How was it?" with three options: thumbs-up (64px `palette.surfaceAlt` circle, lucide `ThumbsUp`), Skip text button in the middle, thumbs-down on the right. Thumb maps to `items.rating` via `{ up: 4, down: 2 }`; skip leaves rating null. Either way, any matching open recs (rows where `to_user_id = me ∧ tmdb_id = X ∧ media_type = Y ∧ status IN ('pending','accepted')`) get transitioned to `status='watched' + watched_via_rec=true`, and `rating_thumb` set if the user picked a thumb — that transition fires the existing `notify_recommendation_watched` trigger so the sender gets the push later. Re-tapping Watched while already watched skips the redundant upsert but reopens the sheet (re-rate). Nested-Pressable pattern for tap-outside-to-dismiss; `onRequestClose` wired to Skip for Android back-button parity. Shared `surfaceUpdateError(err)` helper extracted so the status upsert and rating update both surface PostgrestError fields the same way.
- Friends system shipped end-to-end (`f306e70`). Five files: full Friends tab (`src/app/(tabs)/friends.tsx`) with empty-state CTA buttons → invite/add screens and populated state with pending banner + friends list; `src/app/friends/invite.tsx` showing the user's `https://seen.app/invite/<token>` URL with Copy (`expo-clipboard`) + Share (RN `Share`) + Regenerate (confirmed Alert → `rpc('generate_invite_token')` → `update invite_links`); `src/app/friends/add.tsx` with `@`-prefixed handle input → `profiles.select('id').eq('handle', ...).maybeSingle()` lookup → `friend_requests.insert(...)` (the `can_send_friend_request` policy helper enforces no-friendship and no-reverse-request preconditions, errors surface via Alert); `src/app/friends/requests.tsx` with Incoming + Outgoing sections calling the `accept_friend_request` / `decline_friend_request` RPCs and a direct `friend_requests.delete` for outgoing cancels (DELETE policy allows from_user_id = me); `src/app/friends/[handle].tsx` placeholder stub for the friend detail screen. Friends data flow uses the OR-match lexicographic pattern for friendships + a separate `profiles.in(id, ...)` lookup for display data — two queries, no PostgREST joins. `useFocusEffect` for refresh-on-return on both Friends tab and Requests screen. Invite URL flagged in code as a placeholder until a real domain + Universal Links are wired up.
- Recommend flow shipped (`8512e58`). Added `<Stack.Screen name="title/[mediaType]/[tmdbId]/recommend" options={{ presentation: 'modal' }} />` to root layout (nested dynamic route under the existing title detail — Expo Router resolves both the parent leaf `[tmdbId].tsx` and the child `[tmdbId]/recommend.tsx` cleanly). New modal at `src/app/title/[mediaType]/[tmdbId]/recommend.tsx`: Cancel/Recommend/Send header, small poster + title context at top (fetched via the same `getMovie`/`getTV` so we don't pass display state through URL params), single-select friend list with check-circle on the right and `palette.accentSubtle` row highlight for the active row, multiline TextInput note hard-capped to 500 chars with `n/500` counter, `KeyboardAvoidingView` on iOS so the note input doesn't get covered. Send button (top-right) calls `supabase.rpc('send_recommendation', { to_user_id, tmdb_id, media_type, note })`, surfaces server errors (including the unique-violation 23505 for "already recommended") via the shared `surfaceError` helper, dismisses on success. Empty state when the user has no friends. Added a full-width outline "Recommend to a friend" button (lucide `Send` icon) below the three status action buttons on the title detail screen — entry point into the modal.
- Second EAS dev build of the day to pick up `expo-clipboard` (needed for the invite-link Copy button). Same fingerprint-cached credentials flow as before, no Apple prompts. Build URL: `https://expo.dev/accounts/trrblplsy/projects/seen/builds/3db13b0a-f82d-4464-8abd-41a902eb6641`. Build ran in parallel with the Recommend flow work and finished while the code was being written — efficient use of the cloud build window.
- Friends polish (`57980ba`). Three small fixes: (1) empty-state heading softened from "Add friends to start sharing recs" → "Seen is better with friends. Add yours to get started." with body trimmed to "Share your invite link, or add someone by their handle." (verb aligns with the primary button below). (2) Self-add guard on `add.tsx` — a `useEffect` on mount pre-fetches the current user's handle into local state so `handleSubmit` can short-circuit with `Alert.alert("That's you!", "You can't send a friend request to yourself.")` *before* the target lookup query runs. Server-side `can_send_friend_request` would also reject self-requests, so the client guard is UX-only. (3) Friendlier `42501` message in `surfaceError` — the raw Postgres "new row violates row-level security policy" jargon is replaced with action-oriented copy: "Can't send a request to this user right now. You might already be friends, or there's an existing request between you." Comment in the code calls out that code 42501 on this screen specifically means the friend_requests INSERT was rejected by `can_send_friend_request` (no other RLS-protected mutations happen here).
- Phase 1 navigation refactor (`27a5cc2`). Three coordinated changes in one commit. **Tab reduction**: 5 → 4 tabs (Home / Library / Friends / Profile), the standalone Search tab deleted because its functionality moved into Library. **Library merge**: `(tabs)/library.tsx` now has a search TextInput directly under the header with the same 300ms debounce + stale-result guard + poster-required filter from the old Search screen; an `isSearching = query.length > 0` flag flips the body between the watchlist/watching/watched library view and the search-results view; a Cancel button appears next to the input when non-empty and clears + dismisses the keyboard. Both lists share the same poster/title/meta row layout. **Shared `ScreenHeader`** (`src/components/screen-header.tsx`): 44 px-tall bar with optional back chevron on the left, optional title in the centre, lucide `Bell` icon on the right that routes to `/inbox` (badge appears when `unreadCount > 0`, caps at "9+"). Three-column `flex: 1 / flex: 2 / flex: 1` layout keeps the title centred regardless of side content. Self-contained `SafeAreaView edges={['top']}`, so screens using it drop their own root SafeAreaView in favour of a plain View wrapper. Applied to all four tab screens; their existing manual headers and root SafeAreaViews were removed. `hideBell` prop suppresses the right-side bell — set on the inbox screen itself so the icon doesn't link back to the same place. New `src/app/inbox.tsx` placeholder pushes from the bell with `showBackButton` and a "Coming soon" body. Net: 8 files changed, +531/−466 lines.
- Library search-keyboard dismiss fix (`296c622`). The Pressable wrapper around the results body was already in place from Phase 1 but didn't fire because FlatList's underlying ScrollView absorbs taps inside its bounds before they reach the parent. Added `keyboardDismissMode="on-drag"` + `onScrollBeginDrag={Keyboard.dismiss}` on the FlatList itself so scrolling the results dismisses the keyboard — the natural "I want to see more results" gesture. Comment block in the file explains the layered approach (wrapper Pressable handles non-FlatList taps, FlatList props handle scrolls).
- Phase 2 navigation refactor (`3ca5ef0`). Three pieces. **`useUnreadCount` hook** (`src/hooks/use-unread-count.ts`): returns `{ count, refresh }`, refetches via `useFocusEffect`. Sums three sources — pending incoming recs + pending friend requests + unread `rec_watched`/`friend_accepted` notifications. The `rec_received` and `friend_request` notification kinds are intentionally excluded from the unread count because they'd double-count with the first two sources (the underlying rec/request row already counts; the matching notification would too). Wired into all four tab screens' `ScreenHeader` so the bell badge reflects the live count. **Shared `Avatar` component** (`src/components/avatar.tsx`): coral circle with uppercase first-letter fallback when `avatar_url` is null, sized via a `size` prop. Used by inbox immediately; future refactor can swap the inline avatars in profile/friends/requests for this. **Real inbox** (`src/app/inbox.tsx`, 683 lines replacing the placeholder): three parallel source queries (incoming pending recs, pending friend requests, `rec_watched`/`friend_accepted` notifications, each limited to 50), batched profile lookup, `Promise.allSettled` TMDB title fetches, merged into a discriminated `InboxItem` union sorted by `createdAt desc`. Four row renderers: incoming rec → "Sarah recommended Severance" with note preview, taps to `/title/.../?fromRec=<id>`; friend request → inline Accept/Decline buttons calling the existing RPCs; rec_watched → "Sarah watched X" taps to title detail; friend_accepted → "Sarah is now your friend" taps to `/friends/<handle>`. `relativeTimestamp` helper renders "just now / 5m / 3h / 4d / 5/19/2026" per PRD §7. Mark-as-read fires in parallel with the source queries so the bell badge drops the instant the screen opens; tabs refetch on focus when the user navigates back. **Title detail rec-context card**: new `fromRec` URL param read in the existing useEffect; when set, fetches the recommendation row + sender profile and renders a small `surfaceAlt` card at the very top of the ScrollView with avatar + "Sender recommended this to you" + italic note in quotes. Doesn't affect any other behaviour on the title detail screen. **TS gotcha resolved during build**: `const canNavigate = item.mediaType && item.tmdbId` narrowed to `number | null` and broke a Pressable style array; fixed with `!!(...)` to force boolean.
- Home dashboard built (`a4f3ca7`, ~850 lines on `(tabs)/index.tsx`). Three sections stacked in a ScrollView with pull-to-refresh: **(1) Recent activity** — a single card showing the most recent watching/watched library item, with poster on the left and title on the right; section header reads "Continue watching" or "Last watched" depending on status; hidden entirely when nothing matches. **(2) Friends are watching** — horizontal ScrollView of poster cards from friends' library items (watching/watched, RLS gates non-private only); deduped per `(media_type, tmdb_id)` to one card per title attributed to the most recent watcher's first name; inline "Add friends" CTA when the user has no friends. **(3) Recent recommendations** — vertical list of up to 5 latest recs (sent or received), each row has avatar + title + direction label ("From Sarah" / "To Sarah") + a status pill in one of four colour combos (pending=accentSubtle, accepted=surfaceAlt, watched=success, dismissed=textMuted); tapping a received pending rec routes via `?fromRec=<id>` for the title-detail context card, others go to plain title detail. **Global empty state** — single welcome screen with "Welcome to Seen" + two CTAs ("Search for something to track" → `/library`, "Add a friend" → `/friends`), shown only when `!hasLibraryItems && !hasFriends && recs.length === 0`. **Data flow** is three waves: parallel source queries (activity items, friendships, recs, items-count), then dependent parallel fetches (friend items + rec-other-party profiles), then parallel TMDB title fetches via `Promise.allSettled`. `useFocusEffect` runs the initial load with active-flag stale-guard; the full-screen spinner only shows on initial load (data === null), refresh keeps existing data visible. **Typed-router gotcha**: constructed strings like `\`/title/${mt}/${id}\`` widen to plain `string` and don't pattern-match the `Href` template literal type — switched all `router.push` calls in this file to the object form (`{ pathname: '/title/[mediaType]/[tmdbId]', params: {...} }`). Comment in the helper explains for future readers.
- Home polish (`4b24101`). `ScreenHeader title="Seen"` to give the Home tab a visible wordmark consistent with the other three tabs. Removed the "Pick up where you left off" subtitle from the activity card — it implied stream progress we don't track. The "watching" case now shows just the title; the "watched" case keeps "You watched this on <date>" when `watched_at` is set, falls back to just the title otherwise.

### Session recap (high-level)

**Done**
- Sign-out button on home (temporary, will move to profile/settings).
- TMDB proxy Edge Function with token as Supabase secret, deployed to dev.
- `src/lib/tmdb.ts` client wrapper (typed, calls Edge Function via `supabase.functions.invoke`).
- End-to-end TMDB chain verified: app → Supabase auth → Edge Function → TMDB → typed response.
- Tab structure with five tabs (Home, Library, Search, Friends, Profile — Library/Friends/Profile started as placeholders).
- Real Search screen: TMDB-backed, 300 ms debounce, stale-result guard, expo-image posters, error/empty/loading states, tap-outside-to-dismiss keyboard.
- Filtered search results to only show items with `poster_path` (hides TMDB-database noise).
- Title detail modal at `src/app/title/[mediaType]/[tmdbId].tsx`: backdrop, poster, metadata, three status action buttons that upsert to `items`.
- Real Library screen: three tabs (Watchlist / Watching / Watched), `useFocusEffect` refresh-on-return, `Promise.allSettled` for per-row TMDB metadata with graceful degradation.
- Verified full library loop on device.

**Bug fixes & lessons**
- All six prior migrations enabled RLS but never granted table privileges to `authenticated`. Postgres has two layers (grants AND RLS); we'd only set up the second. The signup trigger worked because it's SECURITY DEFINER and bypasses grants — client mutations failed with `code 42501 permission denied for table items` on the first "add to library" tap.
- Fix: `20260519102336_grant_authenticated_privileges_on_public_tables.sql` granting the right operations per table, matched exactly to the policies they back.
- Patched `rls-auditor` subagent with a new "Grants match policies" check so this can't recur. Added a "Two-layer permission model" preamble to TECHNICAL §2.
- `LinearGradient` native module wasn't in the running dev build (JS package installed but no EAS rebuild yet). Swapped for a plain `<View>` with `opacity: 0.7` until the next dev build picks the module up. TEMP comment in the JSX has the verbatim restore line.

**Next**
- Three rec actions on the title detail screen when arrived via `?fromRec=<id>` — "Already seen / Add to my list / Not for me" — and wire them to the recommendation status transitions (`watched` / `accepted` / `dismissed`). The rec-context card exists; the action buttons are the follow-up.
- Refactor the inline avatars in `profile.tsx`, `(tabs)/friends.tsx`, and `friends/requests.tsx` to use the new `<Avatar />` component — four sites still inline the same 10 lines.
- Onboarding flow (PRD §5 steps 3-7) so first sign-in lands somewhere meaningful instead of the bare Home placeholder.
- Friend detail screen (currently a "Coming soon" placeholder at `src/app/friends/[handle].tsx`) — surface their library, your shared history, an unfriend button.
- Build out the real Edit profile and Account screens (still placeholders behind chevrons in the Profile tab).
- Push notifications wiring: register Expo push token on signup, send via Edge Function with the 5-min per-sender batching from PRD §5.
- `delete_account` / `restore_account` Edge Functions (PRD §5 lifecycle).
- `pg_cron` daily 03:00 UTC job: `hard_delete_expired_accounts` + `prune_stale_push_tokens`.
- Domain + Universal Links so the invite URL `https://seen.app/invite/<token>` actually deep-links into the app and calls `claim_invite_link`. Includes apple-app-site-association + assetlinks.json hosting, `associatedDomains`/`intentFilters` in `app.json`, and an in-app handler.
- Build out the real Edit profile and Account screens (currently just "Coming soon" placeholders behind chevrons in the Profile tab).
- Carrying over from earlier: load DM Sans via `useFonts`; test Google Sign-In on Android dev build (needs SHA-1); optional `REVOKE EXECUTE … FROM PUBLIC` migration on trigger functions.

**Open questions**
- Refresh-on-focus for Library after closing the title detail modal: works empirically but hasn't been stress-tested. May need a Supabase realtime subscription on `items` or manual cache invalidation later.

---

## 2026-05-18

**Done**
- Wrote PRD.md, TECHNICAL.md (data model + RLS rules + screens), DESIGN.md (tokens, motion, type)
- Downgraded the project from Expo SDK 55 → SDK 54 so it runs in Expo Go on a physical iPhone
- Reverted SDK 55-specific call sites: native-tabs `Icon`/`Label` imports, `SymbolView` `name` props, `useColorScheme` null handling
- Added `@expo-google-fonts/dm-sans` (typography per DESIGN.md)
- Enabled Claude Code plugins: `expo` (SDK skills) and `claude-code-setup` (automation recommender)
- Produced automation recommendations for the Foundation phase: Supabase MCP, context7 MCP, `rls-check` and `new-supabase-migration` skills, `.env` block + tsc-on-edit hooks, `rls-auditor` and `supabase-schema-reviewer` subagents — none implemented yet
- `expo-doctor` 17/17 clean; `tsc --noEmit` clean on `main`
- Installed Supabase MCP (read-only) against the dev project
- Created `rls-auditor` subagent (`.claude/agents/rls-auditor.md`) — reviews Supabase migrations for RLS correctness against TECHNICAL §2 / PRD §5
- Created PreToolUse `block-secrets` hook (`.claude/hooks/block-secrets.sh`) — blocks Read/Edit/Write on `.env*`, `*.pem`, `*.key`, SSH keys, `.npmrc`/`.pypirc`/`.netrc`, and any path containing `credentials`/`secrets` (case-insensitive)
- Resolved 5 open questions in PRD/TECHNICAL/DESIGN: TMDB hybrid proxy via `tmdb-proxy` Edge Function, `push_tokens` table, `pg_cron` daily deletion job, canonical theme path (`src/theme/theme.ts`), profile-visibility clarification
- First migration applied: `supabase/migrations/20260518114602_create_profiles_and_handle_history.sql` — creates `public.profiles` and `public.handle_history` with check constraints, indexes, RLS, and the `handle_new_user()` signup trigger (security definer, placeholder handle `user_` + 12 hex chars). Auditor PASS, pushed to remote (`xhzrsdgrgimlrdnyzidr`), verified via `supabase inspect db table-stats --linked`. Committed `3ed64d1`.
- Second migration applied: `supabase/migrations/20260518121215_create_invite_links.sql` — creates `public.invite_links` (PK user_id → profiles, partial unique index on token where revoked_at is null) with own-row SELECT/UPDATE RLS, no INSERT/DELETE policies. Adds `public.generate_invite_token()` (plain SQL, ~96-bit base64url, 16 chars via `extensions.gen_random_bytes(12)`). Extends `handle_new_user()` to also insert an `invite_links` row at signup. Auditor PASS, pushed to remote, verified all six items via `supabase db query --linked` (columns, RLS, policies, functions, trigger body, indexes). Committed `5909f3a`.
- Third migration applied: `supabase/migrations/20260518123418_create_items.sql` — creates `public.items` per TECHNICAL §1 with five constraints (media_type/status/rating-range/rating-only-when-watched/unique `(user_id,tmdb_id,media_type)`), three indexes, and a BEFORE UPDATE trigger calling a new generic `public.set_updated_at()` helper. Adds `public.is_friend_of_auth(uuid)` — plpgsql/stable/security-definer with `set search_path = public`. Wrapped in plpgsql specifically so policy creation succeeds before the `friendships` table exists (plpgsql defers name resolution); first call against a non-owner row will raise until the friendships migration lands, which is the expected window. Four policies: SELECT own-or-(non-private friend), INSERT/UPDATE/DELETE own. Auditor PASS, verified all seven items via `supabase db query --linked`. Committed `0a2acdb`.
- Fourth migration applied: `supabase/migrations/20260518124510_create_friendships_and_friend_requests.sql` — creates `public.friendships` (PK `(user_a_id,user_b_id)` + lexicographic CHECK + index on user_b_id) and `public.friend_requests` (PK uuid + unique `(from,to)` + self-CHECK + index on to_user_id), all FKs ON DELETE CASCADE. Adds `public.can_send_friend_request(uuid)` helper (security-definer, stable) used by the friend_requests INSERT policy to enforce the no-existing-friendship + no-reverse-pending-request preconditions. Adds four security-definer RPCs: `accept_friend_request`, `decline_friend_request`, `unfriend` (does NOT touch recommendations per PRD §5), and `claim_invite_link` (idempotent on existing friendship, supersedes either-direction pending request). Policies: friendships SELECT-only (own-side); friend_requests SELECT/INSERT/DELETE for party. Verified after push that `is_friend_of_auth` now executes against the real friendships table (forced a non-null auth.uid via `set_config('request.jwt.claims', …)`). Auditor PASS, committed `4c8796b`.
- Fifth migration applied: `supabase/migrations/20260518130612_create_recommendations.sql` — creates `public.recommendations` per TECHNICAL §1 with 9 constraints (media_type / status / note≤500 / dismiss-reason-only-when-dismissed / rating_thumb enum / rating_thumb-only-when-watched / no-self / unique `(from,to,tmdb,media)` / FKs), two `(user, status)` indexes, and two BEFORE UPDATE triggers — `enforce_recommendation_immutability` (locks id, from/to_user_id, tmdb_id, media_type, note, sent_at) and `set_recommendation_resolved_at` (sets resolved_at on `pending → non-pending` transitions). Three policies (no DELETE): SELECT party, INSERT sender-to-friend via `is_friend_of_auth`, UPDATE recipient. Adds `send_recommendation(to_user_id, tmdb_id, media_type, note)` security-definer RPC returning the new id. Auditor first pass FAILed — strict immutability trigger blocked the FK `ON DELETE SET NULL` cascade (referential actions fire user BEFORE UPDATE triggers in PG), breaking PRD §5 anonymisation. Fix: trigger now permits the `from_user_id: non-null → NULL` transition only when `current_user not in ('authenticated','anon')` (privileged-role gate — service_role from `delete_account`, postgres from cron). Re-audit PASS. Committed `60b2ffd`.
- Lifecycle correction migration: `supabase/migrations/20260518140000_rename_saved_to_accepted_in_recommendations.sql` — locked the rec lifecycle as `pending → accepted → watched (or dismissed)`. Drops + re-adds `recommendations_status_check` with `'accepted'` replacing `'saved'` (table verified empty, no row UPDATE needed). The auditor caught a knock-on: under the new four-state model, `accepted` means "rec still open", but the existing `set_recommendation_resolved_at` would have stamped `resolved_at` on `pending → accepted`, breaking the `resolved_at IS NULL ↔ open` invariant. Fix: redefined the trigger to stamp only on true terminal transitions `(pending|accepted) → (watched|dismissed)` and defensively clear on the reverse path. Auditor walked the full 12-cell transition matrix and confirmed the invariant holds. TECHNICAL §1 (status enum, resolved_at column note, lifecycle bullets) and PRD §5 (Recommendation lifecycle subsection) updated to match. Committed `a676ea4`.
- Sixth migration applied: `supabase/migrations/20260518150956_create_notifications_and_push_tokens.sql` — creates `public.notifications` (kind CHECK enum of four values, jsonb payload defaulting to `'{}'`, indexes on `(user_id, read_at)` and `(user_id, created_at)`, RLS SELECT/UPDATE own; no INSERT/DELETE policies — server-side only) and `public.push_tokens` (platform CHECK, unique `(user_id, device_id)`, full own-row CRUD RLS). Adds two new security-definer trigger functions: `notify_friend_request_received` (AFTER INSERT on friend_requests → notify `to_user_id`) and `notify_recommendation_watched` (AFTER UPDATE on recommendations → notify `from_user_id` on `(pending|accepted) → watched` transitions, skipping anonymised senders). Full-body replacements of three existing RPCs add notification inserts: `accept_friend_request` (`friend_accepted` for original sender), `claim_invite_link` (`friend_accepted` for link owner, skipped on idempotent re-claim), `send_recommendation` (`rec_received` for recipient). Auditor PASS, all seven verification queries confirmed. Committed `8741b20`.
- **Schema phase complete.** All 9 tables from TECHNICAL §1 are live on the dev project (`xhzrsdgrgimlrdnyzidr`): `profiles`, `handle_history`, `invite_links`, `items`, `friendships`, `friend_requests`, `recommendations`, `notifications`, `push_tokens`. Five notification trigger points wired. All RLS policies match TECHNICAL §2. Defense-in-depth helpers: `is_friend_of_auth`, `can_send_friend_request`. RPCs: `accept_friend_request`, `decline_friend_request`, `unfriend`, `claim_invite_link`, `send_recommendation`, `generate_invite_token`. Generic `set_updated_at` available for reuse.
- PRD §5 copy fix: removed "Save for later" from the dismiss quick-reply sheet. Reason: dismissal is for rejection; what "Save for later" used to mean is now the separate `accepted` state on the rec lifecycle (adds to watchlist with attribution, rec stays open). The dismiss sheet is now: "Not for me" / "Already watched" / custom / silent. No schema change needed — `dismiss_reason` was already free-text. Committed `84a3719`.
- Process note: when rls-auditor returns FAIL with a proposed fix, surface the fix + re-approval request before applying. Same gate as original approval. (Saved as feedback memory.)
- Discovered the Supabase MCP server hasn't been exposing tools in-session; used `supabase db query --linked` (Management API) as the verification path instead. Docker isn't running, so `supabase db dump` doesn't work either — same workaround applies.
- Supabase client + types step: `npx expo install` added `@supabase/supabase-js`, `@react-native-async-storage/async-storage`, `react-native-url-polyfill`. Regenerated `src/lib/database.types.ts` from the live schema (532 lines, all 9 tables and 8 functions present). Wrote `src/lib/env.ts` with fail-fast validation of `EXPO_PUBLIC_SUPABASE_URL` / `EXPO_PUBLIC_SUPABASE_ANON_KEY` (Expo's Babel transform only inlines dot-notation `process.env.X`, so a generic `required(name)` helper would silently break — env.ts reads each var by name then narrows via early-return throw). Wrote `src/lib/supabase.ts` exporting a `createClient<Database>` singleton with AsyncStorage persistence, autoRefreshToken, detectSessionInUrl:false. `react-native-url-polyfill/auto` imported before supabase-js (canonical RN pattern; Supabase auth/realtime URL parsing requires it). Updated TECHNICAL §6 with the corrected type-gen command: the CLI writes status messages to stdout by default and would pollute the output without `--log-level error 2>/dev/null`. `tsc --noEmit` clean. Committed `6598f05`.
- Theme phase: created canonical `src/theme/theme.ts` from DESIGN.md tokens (palette light/dark, typography 6 sizes, spacing 4px scale, radius, motion, elevation, fontFamily, ColorScheme + getPalette). Fixed the type bug from the old `THEME.ts` — `type Palette = typeof palette.light` was broken because `as const` made every color a literal type, so `palette.dark.bg: "#15110F"` failed to satisfy `palette.light.bg: "#FAF7F2"`. Now defined as a structural mapped type `{ readonly [K in keyof typeof palette.light]: string }`. Deleted 15 starter files (constants/theme, app-tabs/web, animated-icon set, themed-text/-view, hint-row, web-badge, external-link, ui/collapsible, use-theme hook, explore screen, old THEME.ts). Rewrote `_layout.tsx` as a 9-line minimal Stack with no header, and `index.tsx` as a 20-line placeholder rendering centred "Seen" via `typography.display` + palette tokens (color scheme via `useColorScheme() ?? 'light'`). Kept `use-color-scheme.ts` / `.web.ts` (not tied to starter theme). `tsc --noEmit` clean; `expo start` loaded the project and `.env.local` without errors before bailing on a port conflict from a separately-running Expo. Net −972 lines. Committed `eefd19f`.
- `.env.local` created (gitignored) holding `EXPO_PUBLIC_SUPABASE_URL` and `EXPO_PUBLIC_SUPABASE_ANON_KEY`. Confirmed loading via `expo start` output. Hook-blocked from Claude reads (block-secrets PreToolUse), so future sessions can't inspect it directly — user is source-of-truth for what's in it.
- app.json updated: `name` set to "Seen" (was lowercase default from create-expo-app), `ios.bundleIdentifier` and `android.package` both set to `com.paultaylor.seen`. `slug` and `scheme` left lowercase (slug is the EAS project identifier, scheme is the deep-link URL scheme — both conventional as lowercase). Committed `e235d75`.
- Auth-provider setup (external state — no record elsewhere in the repo):
  - **Apple**: registered `com.paultaylor.seen` App ID in Apple Developer with Sign in with Apple capability. Configured Apple provider in Supabase (Client IDs field populated; no secret needed for the native Sign in with Apple flow that returns an identity token directly).
  - **Google**: created Google Cloud project "Seen" with OAuth Auth Platform configured (External audience). Created Google iOS OAuth Client (Client ID saved to password manager) and Google Web OAuth Client (Client ID + Secret saved to password manager). Configured Google provider in Supabase with both Client IDs comma-separated, the Web Client Secret, and "Skip nonce checks" enabled.
  - **Skipped for now**: Google Android OAuth Client — needs the app's release SHA-1 fingerprint, which only exists after the first EAS build. Will register post-iOS validation.
- Authentication flow built and verified end-to-end on a physical iPhone:
  - `npx expo install` added `expo-apple-authentication` and `@react-native-google-signin/google-signin`. Google's config plugin auto-registered in `app.json`.
  - `app.json`: added `ios.usesAppleSignIn: true` and converted the Google plugin entry to array form with `iosUrlScheme` (reversed iOS Client ID). Filled the iOS Client ID into `GoogleSignin.configure` in `src/lib/auth.ts`.
  - `src/lib/auth.ts` — three helpers: `signInWithApple` (FULL_NAME + EMAIL scopes, exchanges identity token via `supabase.auth.signInWithIdToken`), `signInWithGoogle` (Play Services check → signIn → getTokens → signInWithIdToken), `signOut`. Both flows let user-cancellation propagate as a typed error code so the screen can swallow it silently.
  - `src/hooks/use-session.ts` — `useSession()` returns a discriminated union `{ status: 'loading' } | { status: 'ready', session }`, subscribed to `onAuthStateChange` (unsubscribed on unmount).
  - `src/app/(auth)/_layout.tsx` + `src/app/(auth)/sign-in.tsx` — Stack group with header off; sign-in screen platform-branches the button (Apple's native `AppleAuthenticationButton` on iOS, themed coral Pressable on Android) under a centred "Seen" wordmark, with a tagline below and disclosure pinned to the bottom. Busy state disables re-tap.
  - `src/app/_layout.tsx` — session-aware routing: `useEffect` on `(session, segments, router)` redirects to `/(auth)/sign-in` when signed out + outside the auth group, and to `/` when signed in + inside it. Loading overlay (ActivityIndicator on `palette.bg`) covers the navigator until the session resolves.
- EAS workflow set up: installed/configured EAS CLI, ran `eas build:configure` for iOS, created and installed the first iOS development build on a physical iPhone.
- End-to-end Sign in with Apple verified on device: signed in → auth.users row created → `profiles` row created via signup trigger → `invite_links` row created via the extended trigger from migration 2. Session-aware routing correctly directs unauthenticated users to `/(auth)/sign-in` and signed-in users back to `/`. Committed `98bda0c`.

**Next**
- Add a temporary sign-out button so we can re-run the auth flow without reinstalling — likely on the home placeholder, in the corner.
- Build the onboarding flow (PRD §5: handle/display-name → last-watched → best-watched → watchlist-three → invite). Triggers fire on first INSERT but onboarding writes display_name + handle over the placeholders.
- Build the TMDB proxy Edge Function (TMDB Read Access Token as Supabase secret) and `src/lib/tmdb.ts` wrapper.
- First real screen pair after onboarding: TMDB search → add to library → library view.
- Test Google Sign-In path: needs an Android dev build (which requires the SHA-1 → Google Android OAuth Client) or a second iOS test device with a Google account configured.

**Open questions**
- None for this phase. Schema phase handoff was clean.
